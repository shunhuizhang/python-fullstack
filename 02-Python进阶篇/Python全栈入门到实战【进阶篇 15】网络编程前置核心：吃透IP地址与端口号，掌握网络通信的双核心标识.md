

# Python全栈入门到实战【进阶篇 15】网络编程前置核心：吃透IP地址与端口号，掌握网络通信的双核心标识
上一篇我们吃透了**协程编程**（超高并发IO的终极方案），正式进入Python**网络编程模块**——这是实现**跨机器/跨网络通信**的核心，也是开发客户端/服务端程序（如聊天工具、文件传输服务、自定义接口）的基础。

而TCP/UDP网络编程的**前置核心基础**，就是**IP地址**和**端口号**：IP地址定位网络中的**设备**，端口号定位设备上的**进程**，二者组合是网络中进程的**唯一标识**，没有这两个核心标识，后续的TCP连接、UDP通信都无从谈起。

本节课作为网络编程的专属前置篇，专门讲透IP地址与端口号的核心知识，**从概念本质到Python开发实战**，摒弃繁琐的网络底层原理，只讲Python开发中**必须掌握的核心点**，同时搭配可直接复用的Python实战代码，帮你扫清TCP/UDP编程的基础障碍，为后续实战做好100%准备。

![](https://liaoxuefeng.com/books/python/network/tcp-ip/net.png)

本节核心学习内容：

- IP地址：核心定义、分类（公网/私网/回环）、Python开发核心常用IP（127.0.0.1/0.0.0.0）的含义
- 端口号：核心定义、范围划分、分类（知名/注册/动态）、端口的核心使用规则
- 核心组合：IP+端口的本质意义，成为网络进程唯一标识的底层逻辑
- 4个Python实战：获取本机IP、解析域名对应IP、检测端口占用、释放占用端口
- 网络编程必知：Socket套接字与IP+端口的关联
- 新手必避的7个IP/端口坑点：绑定回环IP无法访问、端口占用、无权限绑定知名端口等

# 一、IP地址：定位网络中**唯一设备**的「门牌号」
### 1.1 核心定义
IP地址（Internet Protocol Address）是**网络中每个联网设备的唯一数字标识**，作用类似于现实中的**门牌号**——通过IP地址，能在复杂的网络中精准定位到某一台电脑、手机、服务器等联网设备。

简单来说：**网络通信的第一步，是通过IP地址找到要通信的设备**。

### 1.2 主流IP版本：IPv4（重点）与IPv6
目前网络中存在两个IP版本，**Python网络编程中99%的场景使用IPv4**，IPv6为后续拓展，只需了解核心区别：
| IP版本 | 格式                                                      | 地址数量         | 核心应用场景                   | Python编程标识                  |
| ------ | --------------------------------------------------------- | ---------------- | ------------------------------ | ------------------------------- |
| IPv4   | 点分十进制（如192.168.1.100），由4个0-255的数字组成       | 约43亿（已枯竭） | 服务器、本地开发、日常网络通信 | `AF_INET`（Python socket模块）  |
| IPv6   | 冒分十六进制（如2001:0db8:85a3:0000:0000:8a2e:0370:7334） | 无限（理论上）   | 下一代网络、物联网、大型运营商 | `AF_INET6`（Python socket模块） |

**核心重点**：后续的TCP/UDP实战均基于**IPv4**，Python代码中统一使用`AF_INET`标识IPv4协议。

### 1.3 Python开发中**必须掌握**的IPv4分类与常用IP
抛开繁琐的A/B/C/D/E类网络划分，从Python开发视角，只需掌握**公网IP、私网IP、回环IP**三类，以及两个最常用的特殊IP（127.0.0.1/0.0.0.0），覆盖99%的开发场景。
#### 1.3.1 公网IP：互联网中**全局唯一**的设备标识
- 定义：由运营商分配的、可在**互联网中直接访问**的IP地址，全网唯一，不会重复；
- 例子：服务器的公网IP（如120.79.xxx.xxx）、手机连4G/5G的公网IP；
- 开发场景：Python开发的服务端程序部署到云服务器后，需通过**公网IP**对外提供服务。

#### 1.3.2 私网IP：局域网内**唯一**的设备标识
- 定义：仅在**局域网内**有效，无法在互联网中直接访问的IP地址，不同局域网的私网IP可重复；
- 常用私网IP段（开发中最常接触）：
  - 192.168.x.x（最主流，家庭/公司局域网均使用该段）
  - 10.x.x.x（大型企业/机房局域网）
  - 172.16.x.x - 172.31.x.x（中型局域网）
- 开发场景：**本地开发核心场景**，比如在电脑上开发TCP服务端，同一局域网的手机/另一台电脑可通过该电脑的**私网IP**访问服务。

#### 1.3.3 回环IP：127.0.0.1（本地回环地址，核心常用）
- 定义：**仅指向本机**的特殊IP，无论设备是否联网，该IP都有效，用于**本机进程间的通信**；
- 别名：`localhost`（域名），与127.0.0.1一一对应，Python代码中可互换使用；
- 核心特点：通过127.0.0.1访问的服务，**仅本机能访问**，局域网内其他设备无法访问；
- 开发场景：**本地调试核心场景**，比如开发TCP服务端时，先绑定127.0.0.1，仅本机测试，避免局域网其他设备干扰。

#### 1.3.4 0.0.0.0（通配IP，服务端绑定核心IP）
- 定义：**不是一个实际的IP地址**，而是一个**绑定标识**，表示「绑定本机的**所有网络接口**」；
- 核心特点：服务端绑定0.0.0.0后，**本机的公网IP、私网IP、回环IP**均可访问该服务；
- 开发场景：**服务端程序对外提供服务的核心配置**，比如本地开发完成后，绑定0.0.0.0，让局域网内其他设备可通过私网IP访问；云服务器部署服务时，绑定0.0.0.0，让外网可通过公网IP访问。

**核心对比（开发必记）**：
- 服务端绑定`127.0.0.1` → 仅本机可访问（本地调试）；
- 服务端绑定`0.0.0.0` → 本机/局域网/外网（若有公网IP）均可访问（对外提供服务）。

# 二、端口号：定位设备上**唯一进程**的「房间号」
通过IP地址找到目标设备后，还需要找到设备上**要通信的程序（进程）**，而端口号就是定位设备内进程的**唯一标识**，作用类似于门牌号中的**房间号**。

### 2.1 核心定义
端口号（Port Number）是**设备中每个网络进程的唯一数字标识**，是一个**16位的无符号整数**，通过端口号，能在一台设备中精准定位到某一个网络程序（如浏览器、微信、Python开发的TCP服务端）。

核心规则：**一台设备中，同一个端口号**，**同一时间只能被一个进程占用**（类似一个房间只能住一个人），这是网络编程中最核心的端口规则，也是新手最常踩坑的点。

### 2.2 端口号的**范围划分**（固定，必记）
端口号的取值范围是**0 ~ 65535**，根据使用权限和场景，分为三类，Python开发中重点掌握**知名端口**和**动态端口**：
#### 2.2.1 知名端口（0 ~ 1023）：系统/知名服务专用，**普通用户无权限绑定**
- 定义：由IANA（互联网编号分配机构）分配，用于系统核心服务和知名网络服务，**需要管理员/root权限才能绑定**；
- 常用知名端口（开发必记）：
  - 80：HTTP协议默认端口（浏览器访问网页）
  - 443：HTTPS协议默认端口（加密网页访问）
  - 22：SSH协议端口（远程连接服务器）
  - 3306：MySQL数据库默认端口
  - 6379：Redis数据库默认端口
- 开发注意：Python开发中，**不要随意绑定1023以内的端口**，普通用户运行会报「权限不足」错误，除非以管理员/root身份运行。

#### 2.2.2 注册端口（1024 ~ 49151）：开发者可自由使用，无权限限制
- 定义：供开发者自定义网络服务使用的端口，**普通用户可直接绑定**，无权限限制，且不会与系统核心服务冲突；
- 开发场景：**Python网络编程的核心端口范围**，比如开发TCP服务端时，优先选择该段端口（如8080、9090、5000、8888等）。

#### 2.2.3 动态端口（49152 ~ 65535）：系统临时分配，**开发者无需手动绑定**
- 定义：由操作系统**动态分配**给临时的网络进程（如浏览器访问网页、Python客户端程序），进程结束后，端口会被系统回收；
- 开发场景：Python**客户端程序**无需手动指定端口，由系统自动分配即可，开发者无需关注该段端口。

### 2.3 端口号的**核心使用规则**（开发必守）
1. **唯一性**：同一设备，同一端口号只能被一个进程占用，重复绑定会直接失败；
2. **权限性**：0~1023的知名端口需要管理员/root权限才能绑定，普通端口无权限限制；
3. **可回收性**：进程结束后，端口会被系统回收，可再次被其他进程绑定（特殊情况会出现**端口占用未释放**，需手动处理）；
4. **双向性**：网络通信中，客户端和服务端都有端口——服务端绑定**固定端口**，客户端使用系统**动态分配的临时端口**。

# 三、IP地址 + 端口号：网络中**进程的唯一标识**（核心组合）
单独的IP地址只能定位设备，单独的端口号只能定位设备内的进程，**二者组合（IP:端口）**，才能在**整个网络中**定位到**唯一的进程**，这是网络通信的**核心基础**。

### 3.1 组合格式
**IPv4地址:端口号**，如`192.168.1.100:8080`、`127.0.0.1:9090`、`120.79.xxx.xxx:5000`。

### 3.2 核心本质
IP+端口的组合，是**Socket套接字（后续TCP/UDP的核心）**的**地址标识**，网络中任意两个进程的通信，本质就是**一个进程的IP+端口**与**另一个进程的IP+端口**之间的数据传输。

### 3.3 通信示例（直观理解）
Python开发的TCP服务端运行在电脑A（私网IP：192.168.1.100），绑定端口8080 → 服务端标识：`192.168.1.100:8080`；
Python开发的TCP客户端运行在电脑B（私网IP：192.168.1.101），系统分配临时端口56789 → 客户端标识：`192.168.1.101:56789`；
客户端向服务端发起通信，本质就是**192.168.1.101:56789** 与 **192.168.1.100:8080** 建立连接并传输数据。

# 四、Socket套接字：IP+端口的**编程载体**（提前铺垫）
在Python网络编程中，**IP+端口的组合**需要通过**Socket套接字**来封装和使用，Socket是实现TCP/UDP通信的**编程接口**，是Python`socket`模块的核心。

### 核心定义
Socket（套接字）是**网络通信的编程抽象**，封装了**IP地址、端口号、网络协议（TCP/UDP）**三大核心要素，Python通过创建Socket对象，来实现进程间的网络通信。

### 与IP+端口的关系
Socket = **IP地址 + 端口号 + 网络协议**，IP+端口是Socket的**地址核心**，协议是Socket的**通信规则**，后续TCP/UDP编程中，我们会通过Socket对象绑定**IP+端口**，实现数据的发送和接收。

**简单理解**：IP+端口是「通信地址」，Socket是「通信工具」，通过工具绑定地址，实现网络通信。

# 五、Python实战：IP与端口的**核心操作**（代码可直接复用）
基于Python内置的`socket`模块和`os`/`subprocess`模块，实现开发中最常用的4个IP/端口操作，代码跨平台（Windows/Linux/Mac）兼容，带详细注释，可直接复制使用。

### 环境准备
无需安装第三方库，直接使用Python内置模块：
- `socket`：核心模块，实现IP解析、本机IP获取、端口检测；
- `subprocess`：跨平台执行系统命令，实现端口释放；
- `platform`：判断操作系统，实现跨平台兼容。

## 5.1 实战1：获取本机的**私网IP**和**回环IP**（开发最常用）
```python
import socket

def get_local_ip():
    """
    获取本机的私网IP（局域网IP）
    原理：通过连接外部服务器的临时socket，获取本地网卡的IP
    """
    try:
        # 创建临时的TCP socket，连接外部服务器（无需实际连接成功）
        with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
            # 连接百度的公网IP+80端口（仅用于获取本地IP，无实际数据传输）
            s.connect(("180.101.50.188", 80))
            # 获取socket绑定的本地IP
            local_ip = s.getsockname()[0]
        # 回环IP固定为127.0.0.1
        loopback_ip = "127.0.0.1"
        return {
            "私网IP(局域网)": local_ip,
            "回环IP(本机)": loopback_ip,
            "服务端绑定推荐": "0.0.0.0（所有IP可访问）"
        }
    except Exception as e:
        return f"获取本机IP失败：{str(e)}"

# 调用函数
if __name__ == "__main__":
    ip_info = get_local_ip()
    for key, value in ip_info.items():
        print(f"{key}：{value}")
```
### 运行结果（Windows/Linux/Mac）
```
私网IP(局域网)：192.168.1.100
回环IP(本机)：127.0.0.1
服务端绑定推荐：0.0.0.0（所有IP可访问）
```

## 5.2 实战2：解析**域名对应的IP地址**（如百度/谷歌）
开发中常通过域名访问服务（如www.baidu.com），需将域名解析为IP地址，Python`socket`模块可直接实现，替代DNS解析工具。
```python
import socket

def domain_to_ip(domain):
    """
    将域名解析为对应的IP地址
    :param domain: 域名（如www.baidu.com、github.com）
    :return: 解析后的IP列表
    """
    try:
        # gethostbyname_ex：返回域名对应的主机名、别名、IP列表
        host_name, alias_list, ip_list = socket.gethostbyname_ex(domain)
        return {
            "域名": domain,
            "主机名": host_name,
            "解析的IP列表": ip_list
        }
    except socket.gaierror:
        return f"域名解析失败：{domain} 不是有效域名或网络不可达"

# 调用函数
if __name__ == "__main__":
    # 解析百度域名
    print(domain_to_ip("www.baidu.com"))
    print("-" * 50)
    # 解析GitHub域名
    print(domain_to_ip("github.com"))
```
### 运行结果
```
{'域名': 'www.baidu.com', '主机名': 'www.a.shifen.com', '解析的IP列表': ['183.2.172.177', '183.2.172.17']}
--------------------------------------------------
{'域名': 'github.com', '主机名': 'github.com', '解析的IP列表': ['20.205.243.166']}
```

## 5.3 实战3：检测**指定端口是否被占用**（开发核心常用）
开发中绑定端口时，常遇到「端口被占用」的错误，可通过该代码提前检测端口状态，避免绑定失败。
```python
import socket

def check_port(ip, port):
    """
    检测指定IP的指定端口是否被占用
    :param ip: 要检测的IP（如127.0.0.1、0.0.0.0、本机私网IP）
    :param port: 要检测的端口（如8080、9090）
    :return: 端口状态（被占用/未被占用）
    """
    try:
        # 创建socket对象
        with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
            # 设置socket超时时间（避免阻塞）
            s.settimeout(1)
            # 尝试连接IP+端口，能连接则表示端口被占用
            result = s.connect_ex((ip, port))
            # connect_ex返回0表示连接成功，非0表示失败
            if result == 0:
                return f"端口 {ip}:{port} 已被占用"
            else:
                return f"端口 {ip}:{port} 未被占用"
    except Exception as e:
        return f"检测端口失败：{str(e)}"

# 调用函数
if __name__ == "__main__":
    # 检测本机8080端口
    print(check_port("127.0.0.1", 8080))
    # 检测本机3306端口（MySQL默认端口）
    print(check_port("127.0.0.1", 3306))
    # 检测本机私网IP的9090端口
    print(check_port("192.168.1.100", 9090))
```
### 运行结果
```
端口 127.0.0.1:8080 未被占用
端口 127.0.0.1:3306 已被占用
端口 192.168.1.100:9090 未被占用
```

## 5.4 实战4：跨平台**释放被占用的端口**（解决端口占用问题）
若端口被占用且进程未正常结束，会导致端口**被占用未释放**，需手动杀死占用进程，释放端口，以下代码实现**Windows/Linux/Mac**跨平台兼容。
```python
import socket
import platform
import subprocess

def free_port(port):
    """
    跨平台释放被占用的端口：杀死占用该端口的进程
    :param port: 要释放的端口
    :return: 释放结果
    """
    # 先检测端口是否被占用
    with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
        if s.connect_ex(("127.0.0.1", port)) != 0:
            return f"端口 {port} 未被占用，无需释放"
    
    # 获取操作系统类型
    os_type = platform.system()
    try:
        if os_type == "Windows":
            # Windows系统：通过netstat找到占用端口的PID，再通过taskkill杀死进程
            # netstat -ano | findstr :端口号 → 找到PID
            cmd1 = f"netstat -ano | findstr :{port}"
            result1 = subprocess.check_output(cmd1, shell=True, encoding="gbk")
            # 提取PID（最后一列是PID）
            pid = result1.strip().split()[-1]
            # taskkill /F /PID 进程号 → 强制杀死进程
            cmd2 = f"taskkill /F /PID {pid}"
            subprocess.check_output(cmd2, shell=True)
            return f"Windows系统：成功杀死占用端口{port}的进程，PID：{pid}"
        elif os_type in ["Linux", "Darwin"]:
            # Linux/Mac系统：通过lsof找到占用端口的PID，再通过kill -9杀死进程
            # lsof -i:端口号 -t → 仅输出PID
            cmd1 = f"lsof -i:{port} -t"
            pid = subprocess.check_output(cmd1, shell=True, encoding="utf-8").strip()
            # kill -9 进程号 → 强制杀死进程
            cmd2 = f"kill -9 {pid}"
            subprocess.check_output(cmd2, shell=True)
            return f"{os_type}系统：成功杀死占用端口{port}的进程，PID：{pid}"
        else:
            return f"不支持的操作系统：{os_type}"
    except Exception as e:
        return f"释放端口{port}失败：{str(e)}"

# 调用函数
if __name__ == "__main__":
    # 释放本机8080端口
    print(free_port(8080))
    # 释放本机3306端口
    print(free_port(3306))
```
### 运行结果（Windows示例）
```
端口 8080 未被占用，无需释放
Windows系统：成功杀死占用端口3306的进程，PID：1234
```

# 六、网络编程必避的7个IP/端口坑点（重点，避免踩雷）
新手在后续TCP/UDP编程中，80%的错误都与IP/端口相关，以下是最常踩的7个坑点，附详细避坑方案，看完直接绕开99%的基础问题。

### 坑1：服务端绑定127.0.0.1，局域网其他设备无法访问
**问题**：服务端绑定127.0.0.1后，仅本机能访问，局域网内的手机/另一台电脑通过私网IP访问时提示「连接失败」；
**避坑**：需要对外提供服务时，服务端**绑定0.0.0.0**，而非127.0.0.1。

### 坑2：绑定0.0.0.0后，本机访问时用公网IP导致失败
**问题**：服务端绑定0.0.0.0，本机用公网IP访问时失败，而私网IP/127.0.0.1可正常访问；
**避坑**：本机访问绑定0.0.0.0的服务，**优先用127.0.0.1或私网IP**，无需用公网IP（无公网IP的情况下，公网IP访问本身就会失败）。

### 坑3：绑定1023以内的知名端口，提示「权限不足」
**问题**：Python代码绑定80/443/22等知名端口时，报「PermissionError: [Errno 13] Permission denied」；
**避坑**：1. 优先选择1024以上的普通端口（如8080、9090）；2. 若必须绑定知名端口，需以**管理员/root**身份运行Python程序。

### 坑4：端口被占用，绑定时报「Address already in use」
**问题**：绑定端口时，报「OSError: [Errno 98] Address already in use」（Linux）或「OSError: [Errno 10048]」（Windows）；
**避坑**：1. 用**实战3的代码**检测端口占用状态；2. 用**实战4的代码**杀死占用进程，释放端口；3. 更换未被占用的端口。

### 坑5：进程结束后，端口仍被占用（TIME_WAIT状态）
**问题**：服务端进程正常结束后，立即绑定同一端口，仍提示端口被占用（Linux/Mac明显）；
**原因**：TCP协议的**TIME_WAIT**状态，端口会被系统保留约1-2分钟，防止数据残留；
**避坑**：1. 等待1-2分钟后再绑定；2. Python代码中给socket设置**SO_REUSEADDR**选项，允许复用端口（后续TCP实战会详细讲解）。

### 坑6：服务端绑定具体私网IP，更换网络后无法访问
**问题**：服务端绑定具体的私网IP（如192.168.1.100），更换网络后（如连接另一WiFi，私网IP变为192.168.2.100），服务无法访问；
**避坑**：无论是否更换网络，服务端**绑定0.0.0.0**即可，自动适配本机的所有IP。

### 坑7：客户端程序手动绑定固定端口，导致端口冲突
**问题**：Python客户端程序手动绑定固定端口，运行多个客户端时提示端口被占用；
**避坑**：**客户端程序无需手动绑定端口**，由操作系统自动分配动态端口即可，避免端口冲突。

# 七、核心总结
本节课作为Python网络编程的前置核心，我们吃透了**IP地址**和**端口号**的核心知识，这是后续TCP/UDP编程的**基础中的基础**，核心要点回顾，务必记牢：
1. **IP地址**：定位网络中的唯一设备，开发中重点掌握IPv4，核心常用IP：
   - 127.0.0.1：回环IP，仅本机可访问（本地调试）；
   - 0.0.0.0：通配IP，服务端绑定后，所有IP均可访问（对外提供服务）；
   - 私网IP：局域网内访问，公网IP：互联网中访问。
2. **端口号**：定位设备内的唯一网络进程，取值范围0-65535：
   - 0-1023：知名端口，需管理员权限，避免绑定；
   - 1024-49151：普通端口，开发者核心使用范围；
   - 核心规则：同一端口同一时间只能被一个进程占用。
3. **核心组合**：IP+端口（如192.168.1.100:8080）是网络中进程的唯一标识，是Socket套接字的地址核心。
4. **Socket套接字**：封装了IP+端口+网络协议，是Python实现TCP/UDP通信的核心编程接口。
5. **开发实战**：会获取本机IP、解析域名到IP、检测端口占用、释放被占用端口，解决开发中最常见的IP/端口问题。
6. **核心避坑**：服务端对外提供服务绑定0.0.0.0、避免绑定知名端口、客户端不手动指定端口、端口占用及时释放。

至此，我们已经掌握了网络编程的**前置核心基础**，下一篇我们将正式进入**TCP编程实战**，基于IP+端口的基础，从TCP的核心通信流程出发，实现单客户端-服务端通信、多客户端并发通信等实战案例，将本节课的知识落地到实际代码中。

# 八、专栏订阅
> - <font color=black size=3>==专栏优点？==《Python从入门到实战》，专栏内容涵盖：Python基础到高级编程、并发编程（进程/线程/协程）、网络编程（TCP/UDP/Socket）、Web开发（Django/Flask/FastAPI框架）、数据库（MySQL/ORM/异步数据库）、网络爬虫（同步/异步/分布式）、Linux部署运维等全栈核心知识，以项目驱动教学，构建清晰学习路径，适合零基础入门和进阶提升的同学，跟着一步步从入门到精通！专栏地址：<https://blog.csdn.net/zsh_1314520/category_13108073.html></font>
> - <font color=black size=3>==文章是永久吗？== 一次订阅后可永久免费查看专栏内所有文章，后续会持续更新全栈相关内容，第一时间获取最新教程！</font>
> - <font color=black size=3>==有答疑交流群吗？== 订阅专栏后有专属的全栈学习答疑群，群内提供专业问题答疑、和众多学习者抱团取暖，一起沉淀技术、赋能成长！</font>
> - <font color=black size=3>==进群方式？== 订阅专栏后可直接在专栏内申请加入答疑群，或私信博主沟通进群事宜：<https://bbs.csdn.net/topics/620104702></font>
> - <font color=black size=3>==更多干货？== 点赞+收藏+关注博主不迷路！博主博客链接：<https://blog.csdn.net/zsh_1314520?spm=1000.2115.3001.5343>，专注Python全栈技术分享，评论区留言问题会一一回复，助力大家轻松搞定Python全栈！</font>

【原创声明】

除本文原文地址以外，如发现同款内容皆为盗版，本文已收录于《[Python全栈：从入门到实战](https://blog.csdn.net/zsh_1314520/category_13108073.html)》 ，请勿购买盗版文章和专栏，如购买盗版内容不提供任何服务。原文地址：<https://blog.csdn.net/zsh_1314520/article/details/160550744>
