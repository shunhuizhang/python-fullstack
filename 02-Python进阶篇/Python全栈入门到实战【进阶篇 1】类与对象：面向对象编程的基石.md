

# Python全栈入门到实战【进阶篇 1】类与对象：面向对象编程的基石
在前序内容中，我们完成了Python**基础语法体系的全部学习**（基础篇1-26），从变量、数据类型的基础语法，到函数、闭包、装饰器的函数式编程，再到模块、包的工程化封装，最后掌握了pip第三方库的全流程管理。至此，你已能使用**面向过程编程**实现各类功能，但面对复杂业务场景（如电商系统、管理平台、框架开发）时，面向过程的代码会出现**耦合度高、复用性差、维护成本高**的问题。

从本篇开始，我们正式进入Python**进阶核心板块**——**面向对象编程（OOP）**，这是Python工程化开发、框架源码编写（Django/Flask）、大型项目开发的必备思想，也是从“Python新手”到“Python开发工程师”的关键跨越。

面向对象编程的核心是**以“对象”为中心组织代码**，将数据和操作数据的方法封装为一个整体，通过**类与对象**实现代码的解耦、复用与扩展。本次我们聚焦**面向对象的基石——类与对象**，不掺杂复杂的封装、继承、多态，只吃透最核心的基础：从思维转变到类的定义、对象的创建，从`self`的本质到构造函数`__init__`，从实例属性到类属性，让你彻底建立面向对象的基础思维，为后续的封装、继承、多态打下坚实基础。

本节核心学习内容：
- 面向过程vs面向对象：核心思维差异，理解为什么需要面向对象；
- 类与对象的基础认知：生活类比理解“类是模板，对象是实例”；
- 核心语法：类的定义、对象的创建、实例属性与实例方法；
- 核心难点：彻底吃透`self`的本质与作用，解决新手最大困惑；
- 实战核心：构造函数`__init__`的使用，实现对象的初始化；
- 属性区分：类属性与实例属性的定义、访问与核心区别；
- 实战案例：编写自定义类，实现基础的面向对象编程；
- 新手避坑：`self`遗漏、属性混淆、构造函数参数错误等高频问题。

# 一、先搞懂：面向过程 vs 面向对象，核心思维的转变
学习面向对象的第一步，不是记语法，而是**转变编程思维**。面向过程和面向对象是两种截然不同的代码组织方式，对应不同的业务场景，我们通过**同一个需求的两种实现方式**，直观感受二者的差异。

## 需求：实现一个简单的学生信息管理，包含「查看学生信息」和「修改学生年龄」功能
### 1. 面向过程实现：以“步骤”为中心，代码碎片化
面向过程编程的核心是**按功能拆分子步骤**，用“变量+函数”实现每个步骤，数据（学生信息）和操作数据的方法（查看、修改）是分离的。
```python
# 面向过程：数据与方法分离
# 1. 定义数据（学生信息）
stu_name = "张三"
stu_age = 20
stu_gender = "男"

# 2. 定义操作数据的方法（函数）
def show_stu_info(name, age, gender):
    """查看学生信息"""
    print(f"学生信息：姓名{name}，年龄{age}，性别{gender}")

def update_stu_age(age, new_age):
    """修改学生年龄"""
    age = new_age
    return age

# 3. 按步骤执行功能
show_stu_info(stu_name, stu_age, stu_gender)
stu_age = update_stu_age(stu_age, 21)
show_stu_info(stu_name, stu_age, stu_gender)
```
**面向过程的问题**：
- 数据和方法分离，若要新增学生，需重新定义变量+修改所有函数，**复用性极差**；
- 函数需要传递大量参数，若数据字段增多（如学号、班级），所有函数都要修改，**维护成本高**；
- 代码碎片化，无统一的组织方式，**耦合度高**。

### 2. 面向对象实现：以“对象”为中心，数据与方法封装
面向对象编程的核心是**将数据和操作数据的方法封装为一个整体（对象）**，学生的信息（姓名、年龄）和行为（查看信息、修改年龄）属于同一个“学生对象”，无需分离处理。
```python
# 面向对象：数据与方法封装为整体（类/对象）
# 1. 定义学生类（模板），封装数据和方法
class Student:
    # 初始化学生数据（属性）
    def __init__(self, name, age, gender):
        self.name = name
        self.age = age
        self.gender = gender

    # 定义学生行为（方法），直接操作内部数据
    def show_info(self):
        print(f"学生信息：姓名{self.name}，年龄{self.age}，性别{self.gender}")

    def update_age(self, new_age):
        self.age = new_age

# 2. 创建对象（实例），直接调用方法实现功能
stu1 = Student("张三", 20, "男")
stu1.show_info()
stu1.update_age(21)
stu1.show_info()

# 新增学生，直接创建新对象即可，无需修改类，复用性极强
stu2 = Student("李四", 19, "女")
stu2.show_info()
```
**面向对象的优势**：
- **封装性**：数据（属性）和方法（行为）封装为整体，逻辑清晰，代码更易读；
- **复用性**：新增对象只需通过类创建，无需重复编写代码；
- **可维护性**：修改对象内部数据，只需修改类的方法，外部调用无需变动；
- **扩展性**：后续可通过继承、多态轻松扩展功能（如定义大学生类、中学生类）。

### 核心结论
- **面向过程**：适合简单、一次性的功能实现，核心是“**做什么，分几步做**”；
- **面向对象**：适合复杂、需要复用和扩展的项目开发，核心是“**谁来做，做什么**”（对象来做，调用对象的方法）。

# 二、类与对象的基础认知：生活类比，一看就懂
面向对象的两个核心概念——**类（Class）** 和**对象（Object/Instance）**，是所有面向对象语法的基础，用生活中的案例类比，新手能瞬间理解：

### 1. 类（Class）：事物的**通用模板**
类是对**同一类事物的共同特征和行为的抽象描述**，是一个**通用模板**，不指向具体的事物，比如汽车的图纸。

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/1e6444d17b0946dc84a5d5feebd4a202.jpeg#pic_center)


- 生活案例：**人类**、**汽车类**、**手机类**、**学生类**；
- 核心组成：**属性**（事物的特征，如人类的姓名、年龄、身高；汽车的品牌、颜色、排量）、**方法**（事物的行为，如人类的吃饭、走路；汽车的启动、刹车）。

### 2. 对象（Object/Instance）：类的**具体实例**
对象是根据**类模板创建的具体事物**，是类的**实例**，每个对象都拥有类定义的所有属性和方法，且属性值可以不同，比如根据汽车图纸所制作的汽车。

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/c2fcaf3232c84902b2063aa5a0b633de.jpeg#pic_center)


- 生活案例：人类的具体实例**张三、李四**；汽车类的具体实例**张三的特斯拉、李四的宝马**；学生类的具体实例**stu1、stu2**；
- 核心特征：每个对象都是**独立的**，修改一个对象的属性，不会影响其他对象。

### 类与对象的关系
**类是模板，对象是根据模板创建的具体产品**；**类是抽象的，对象是具体的**；**一个类可以创建无数个对象**。

比如：手机类是模板（属性：品牌、型号、价格；方法：打电话、发消息），你手里的苹果15、我手里的华为Mate60，都是手机类的**具体对象**，它们拥有手机类的所有属性和方法，但属性值（品牌、型号）不同。

# 三、核心语法1：定义类与创建对象，最基础的面向对象操作
Python中定义类使用**`class`关键字**，创建对象使用**`类名()`**（类似调用函数），语法简洁，我们从最基础的空类开始，逐步拆解。

## 1. 定义类：`class`关键字的基础用法
### 语法格式
```python
# 定义类：Class首字母大写（PEP8编码规范，区分普通变量/函数）
class 类名:
    # 类的内容：包含属性和方法
    属性定义
    方法定义
```
### 基础示例：定义一个空的学生类
```python
# 定义空的学生类，pass表示占位，无实际内容
class Student:
    pass
```
**编码规范**：类名遵循**大驼峰命名法**（每个单词首字母大写，无下划线），如`Student`、`Car`、`Phone`，这是Python官方推荐的PEP8规范，便于区分类和其他标识符。

## 2. 创建对象：类的实例化
创建对象的过程称为**类的实例化**，使用`类名()`即可创建类的具体对象，可创建多个独立对象。
### 语法格式
```python
# 创建对象：类名()，将对象赋值给变量，通过变量操作对象
对象名1 = 类名()
对象名2 = 类名()
```
### 基础示例：通过学生类创建多个对象
```python
class Student:
    pass

# 创建学生对象（实例化）
stu1 = Student()
stu2 = Student()

# 打印对象，查看对象的内存地址（每个对象地址不同，说明相互独立）
print(stu1)  # 输出：<__main__.Student object at 0x0000021F78E2A1D0>
print(stu2)  # 输出：<__main__.Student object at 0x0000021F78E2A210>
```
**核心点**：每个对象都有唯一的**内存地址**，说明对象是相互独立的，修改一个对象的属性，不会影响其他对象。

## 3. 为对象动态添加属性（新手入门用法）
创建空对象后，可通过`对象名.属性名 = 属性值`为**单个对象**动态添加属性，这是Python的灵活特性（后续会讲更规范的属性定义方式`__init__`）。
```python
class Student:
    pass

# 创建对象
stu1 = Student()
stu2 = Student()

# 为stu1动态添加属性：姓名、年龄、性别
stu1.name = "张三"
stu1.age = 20
stu1.gender = "男"

# 为stu2动态添加属性：属性值与stu1不同
stu2.name = "李四"
stu2.age = 19
stu2.gender = "女"

# 访问对象的属性：对象名.属性名
print(stu1.name, stu1.age)  # 输出：张三 20
print(stu2.name, stu2.age)  # 输出：李四 19
```
**注意**：这种动态添加属性的方式**仅对当前对象生效**，其他对象不会拥有该属性，若访问未添加的属性，会抛出`AttributeError`异常。

# 四、核心语法2：实例方法与`self`，彻底吃透新手最大难点
方法是**定义在类中的函数**，是对象的**行为**，Python中最基础的方法是**实例方法**（后续会讲类方法、静态方法），而`self`是实例方法的**第一个必传参数**，也是新手学习面向对象的**最大难点**，本节用“原理+案例”彻底讲透。

## 1. 实例方法：属于对象的方法
实例方法是**绑定到对象的方法**，只有创建对象后，才能通过**对象名.方法名()** 调用，是面向对象中最常用的方法。
### 定义规则
1. 实例方法**必须定义在类内部**；
2. 实例方法的**第一个参数必须是`self`**（名称可自定义，如`this`，但Python约定俗成用`self`）；
3. 调用实例方法时，**无需手动传递`self`参数**，Python解释器会自动将**当前对象**传递给`self`。

### 语法示例：为学生类定义实例方法
```python
class Student:
    # 定义实例方法：查看学生信息（第一个参数为self）
    def show_info(self):
        # self代表当前对象，可访问对象的属性
        print(f"姓名：{self.name}，年龄：{self.age}，性别：{self.gender}")

    # 定义实例方法：修改学生年龄（带自定义参数）
    def update_age(self, new_age):
        self.age = new_age

# 创建对象并动态添加属性
stu1 = Student()
stu1.name = "张三"
stu1.age = 20
stu1.gender = "男"

# 调用实例方法：无需传递self，Python自动传递
stu1.show_info()  # 输出：姓名：张三，年龄：20，性别：男
stu1.update_age(21)  # 传递自定义参数new_age
stu1.show_info()  # 输出：姓名：张三，年龄：21，性别：男
```

## 2. `self`的本质：**当前对象的引用**
`self`的核心本质是：**代表调用该方法的「当前对象」，是当前对象的内存地址引用**，通过`self`可以在类的内部：
- 访问当前对象的**属性**（`self.属性名`）；
- 调用当前对象的**其他实例方法**（`self.方法名()`）；
- 初始化当前对象的属性（后续`__init__`中核心用法）。

### 直观理解`self`：打印`self`的内存地址
```python
class Student:
    def check_self(self):
        # 打印self的内存地址
        print(f"self的地址：{id(self)}")

stu1 = Student()
print(f"stu1的地址：{id(stu1)}")  # 输出：stu1的地址：2345678901234
stu1.check_self()                # 输出：self的地址：2345678901234

stu2 = Student()
print(f"stu2的地址：{id(stu2)}")  # 输出：stu2的地址：2345678905678
stu2.check_self()                # 输出：self的地址：2345678905678
```
**核心结论**：`self`的内存地址和**调用方法的对象**的内存地址**完全一致**，说明`self`就是当前对象的引用。

# 五、核心语法3：构造函数`__init__`，对象属性的标准化初始化
在前文中，我们通过`对象名.属性名 = 属性值`为对象动态添加属性，这种方式**不规范、易遗漏**（若忘记添加属性，调用方法会报错）。Python为类提供了**构造函数`__init__`**，用于**对象创建时自动初始化属性**，是面向对象编程中**最核心、最常用**的方法。

## 1. `__init__`的基础认知
- `__init__`是Python的**内置特殊方法**（双下划线开头+双下划线结尾，又称“魔法方法”）；
- 作用：**当创建类的对象时，Python解释器会自动调用`__init__`方法**，为对象初始化属性；
- 特点：无需手动调用，创建对象时自动执行；可自定义参数，实现属性的灵活初始化；
- 注意：`__init__`方法**没有返回值**（不能写`return`），否则会抛出`TypeError`。

## 2. 基本用法：自定义`__init__`，初始化对象属性
### 语法示例：为学生类定义`__init__`，标准化初始化属性
```python
class Student:
    # 构造函数：创建对象时自动调用，初始化属性
    # self为必传参数，后续为自定义参数（姓名、年龄、性别）
    def __init__(self, name, age, gender):
        # 将传入的参数赋值给对象的属性，self.属性名 = 形参
        self.name = name
        self.age = age
        self.gender = gender

    # 实例方法：通过self访问初始化后的属性
    def show_info(self):
        print(f"姓名：{self.name}，年龄：{self.age}，性别：{self.gender}")

# 创建对象：必须传递__init__定义的自定义参数（self无需传递）
stu1 = Student("张三", 20, "男")
stu2 = Student("李四", 19, "女")

# 直接调用方法，无需手动添加属性，避免遗漏
stu1.show_info()  # 输出：姓名：张三，年龄：20，性别：男
stu2.show_info()  # 输出：姓名：李四，年龄：19，性别：女
```
### 调用逻辑
```
stu1 = Student("张三", 20, "男") 
↓
Python自动执行：Student.__init__(stu1, "张三", 20, "男")
↓
self.name = "张三" → stu1.name = "张三"
self.age = 20 → stu1.age = 20
self.gender = "男" → stu1.gender = "男"
```

## 3. 进阶用法：为`__init__`参数设置默认值
为`__init__`的自定义参数设置**默认值**，创建对象时可选择性传递参数，提升灵活性，适合有默认值的属性（如性别、班级）。
```python
class Student:
    # 为gender设置默认值"男"，为class_name设置默认值"一年级一班"
    def __init__(self, name, age, gender="男", class_name="一年级一班"):
        self.name = name
        self.age = age
        self.gender = gender
        self.class_name = class_name

    def show_info(self):
        print(f"姓名：{self.name}，年龄：{self.age}，性别：{self.gender}，班级：{self.class_name}")

# 1. 传递必选参数（name, age），使用默认值（gender, class_name）
stu1 = Student("张三", 20)
stu1.show_info()  # 输出：姓名：张三，年龄：20，性别：男，班级：一年级一班

# 2. 传递部分可选参数，覆盖默认值
stu2 = Student("李四", 19, "女")
stu2.show_info()  # 输出：姓名：李四，年龄：19，性别：女，班级：一年级一班

# 3. 传递所有参数，全部覆盖默认值（可使用关键字参数，更清晰）
stu3 = Student("王五", 21, gender="男", class_name="二年级三班")
stu3.show_info()  # 输出：姓名：王五，年龄：21，性别：男，班级：二年级三班
```

# 六、核心区分：类属性 vs 实例属性，避免新手混淆
在Python的类中，属性分为**类属性**和**实例属性**，二者的定义位置、访问方式、作用范围完全不同，是新手最容易混淆的知识点，也是面试高频考点，我们通过“定义+案例+对比”彻底区分。

## 1. 实例属性：属于**单个对象**的属性
### 定义位置
在**`__init__`方法**中定义，通过`self.属性名 = 属性值`赋值，是最常用的属性类型。
### 作用范围
**仅属于当前对象**，每个对象的实例属性相互独立，修改一个对象的实例属性，**不会影响其他对象和类**。
### 访问方式
**只能通过对象访问**（`对象名.属性名`），不能通过类访问（会报错）。

## 2. 类属性：属于**类本身**的属性，所有对象共享
### 定义位置
直接定义在**类的内部、所有方法之外**，是类的全局属性。
### 作用范围
**属于类本身**，所有通过该类创建的对象**共享同一个类属性**，修改类属性，**所有对象的该属性都会同步改变**。
### 访问方式
**既可以通过类访问**（`类名.属性名`），**也可以通过对象访问**（`对象名.属性名`），推荐通过类访问（更清晰）。

## 3. 对比示例：直观理解二者的区别
```python
class Student:
    # 类属性：定义在类内部，方法之外，所有学生共享
    school = "北京大学"  # 所有学生的学校都是北京大学

    # 实例属性：定义在__init__中，每个学生独有
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def show_info(self):
        # 同时访问类属性和实例属性
        print(f"学校：{Student.school}，姓名：{self.name}，年龄：{self.age}")

# 创建两个对象
stu1 = Student("张三", 20)
stu2 = Student("李四", 19)

# 1. 访问属性
print("=== 初始属性 ===")
# 类访问类属性
print(Student.school)  # 输出：北京大学
# 对象访问实例属性
print(stu1.name, stu2.name)  # 输出：张三 李四
# 对象访问类属性（共享）
print(stu1.school, stu2.school)  # 输出：北京大学 北京大学

# 2. 修改实例属性：仅影响当前对象
print("\n=== 修改实例属性 ===")
stu1.name = "张三三"
stu1.show_info()  # 输出：学校：北京大学，姓名：张三三，年龄：20
stu2.show_info()  # 输出：学校：北京大学，姓名：李四，年龄：19

# 3. 修改类属性：所有对象同步改变
print("\n=== 修改类属性 ===")
Student.school = "清华大学"
stu1.show_info()  # 输出：学校：清华大学，姓名：张三三，年龄：20
stu2.show_info()  # 输出：学校：清华大学，姓名：李四，年龄：19
```

## 4. 关键避坑：对象“修改”类属性的假象
若通过`对象名.类属性名 = 新值`的方式操作类属性，**并不是修改类属性**，而是为**当前对象动态添加了一个同名的实例属性**，类属性本身不会改变，其他对象访问的还是原来的类属性。
```python
class Student:
    school = "北京大学"  # 类属性
    def __init__(self, name):
        self.name = name

stu1 = Student("张三")
stu2 = Student("李四")

# 错误：通过对象给类属性赋值，实际是添加实例属性
stu1.school = "复旦大学"
print(stu1.school)  # 输出：复旦大学（stu1的实例属性）
print(stu2.school)  # 输出：北京大学（类属性，未改变）
print(Student.school)  # 输出：北京大学（类属性，未改变）
```
**结论**：修改类属性**必须通过类**（`类名.类属性名 = 新值`），而非对象。

## 5. 类属性与实例属性的核心对比表
| 对比维度 | 类属性                                    | 实例属性                             |
| -------- | ----------------------------------------- | ------------------------------------ |
| 定义位置 | 类内部、所有方法之外                      | `__init__`方法中（`self.属性名`）    |
| 所属对象 | 类本身                                    | 单个具体对象                         |
| 作用范围 | 所有对象共享                              | 仅当前对象独有，相互独立             |
| 访问方式 | 类名.属性名 / 对象名.属性名（推荐类访问） | 仅能通过对象名.属性名                |
| 修改方式 | 仅能通过类名.属性名 = 新值                | 对象名.属性名 = 新值                 |
| 内存占用 | 类加载时创建，仅占一份内存                | 每个对象创建时都要分配内存，各占一份 |

# 七、实战案例：编写一个汽车类，综合运用所有基础知识点
结合本节所有核心内容，编写一个**汽车类（Car）**，包含类属性、实例属性、实例方法，实现汽车的**属性初始化**、**查看汽车信息**、**启动汽车**、**修改车速**等功能，综合巩固类与对象的基础用法。

## 1. 需求分析
1. 类属性：`brand`（汽车品牌，所有对象共享为“比亚迪”）、`total_cars`（统计创建的汽车总数，初始为0）；
2. 实例属性：`model`（车型）、`color`（颜色）、`speed`（车速，初始为0）；
3. 实例方法：
   - `show_car_info()`：查看汽车的品牌、车型、颜色、车速；
   - `start()`：启动汽车，打印“汽车启动成功”，设置车速为20；
   - `change_speed(new_speed)`：修改车速，设置为指定值；
4. 构造函数：创建汽车时自动初始化车型、颜色，且汽车总数`total_cars`自动加1。

## 2. 代码实现
```python
class Car:
    # 类属性：品牌共享为比亚迪，总数初始为0
    brand = "比亚迪"
    total_cars = 0

    # 构造函数：初始化实例属性，总数自增
    def __init__(self, model, color):
        self.model = model  # 车型
        self.color = color  # 颜色
        self.speed = 0      # 车速，初始为0
        Car.total_cars += 1 # 每创建一辆车，总数加1

    # 实例方法：查看汽车信息
    def show_car_info(self):
        print(f"【汽车信息】品牌：{Car.brand}，车型：{self.model}，颜色：{self.color}，当前车速：{self.speed}km/h")

    # 实例方法：启动汽车
    def start(self):
        print(f"{self.color}色{self.model}启动成功！")
        self.speed = 20  # 启动后车速为20

    # 实例方法：修改车速
    def change_speed(self, new_speed):
        # 简单校验车速：非负数
        if new_speed >= 0:
            self.speed = new_speed
            print(f"车速修改成功，当前车速：{self.speed}km/h")
        else:
            print("车速不能为负数，修改失败！")

# 实战：创建多个汽车对象，调用方法
print("=== 创建第一辆汽车 ===")
car1 = Car("汉EV", "红色")
car1.show_car_info()
car1.start()
car1.change_speed(80)
car1.show_car_info()

print("\n=== 创建第二辆汽车 ===")
car2 = Car("宋Pro", "白色")
car2.show_car_info()
car2.start()
car2.change_speed(100)
car2.show_car_info()

# 查看类属性：汽车总数
print(f"\n当前已创建{Car.total_cars}辆{Car.brand}汽车")
```

## 3. 运行结果
```
=== 创建第一辆汽车 ===
【汽车信息】品牌：比亚迪，车型：汉EV，颜色：红色，当前车速：0km/h
红色色汉EV启动成功！
车速修改成功，当前车速：80km/h
【汽车信息】品牌：比亚迪，车型：汉EV，颜色：红色，当前车速：80km/h

=== 创建第二辆汽车 ===
【汽车信息】品牌：比亚迪，车型：宋Pro，颜色：白色，当前车速：0km/h
白色色宋Pro启动成功！
车速修改成功，当前车速：100km/h
【汽车信息】品牌：比亚迪，车型：宋Pro，颜色：白色，当前车速：100km/h

当前已创建2辆比亚迪汽车
```

# 八、避坑大全：类与对象的高频错误与解决方案
本节整理了新手学习类与对象时的**5个高频错误**，包含错误示例、报错原因、解决方案，帮你避开90%的入门坑。

## 避坑1：实例方法遗漏`self`参数
### 错误示例
```python
class Student:
    def show_info():  # 遗漏self参数
        print("学生信息")

stu = Student()
stu.show_info()  # 报错：TypeError: show_info() takes 0 positional arguments but 1 was given
```
### 报错原因
调用实例方法时，Python会自动传递当前对象给第一个参数，方法未定义`self`，接收不到该参数。
### 解决方案
所有实例方法的**第一个参数必须定义为self**。

## 避坑2：调用实例方法时手动传递`self`
### 错误示例
```python
class Student:
    def __init__(self, name):
        self.name = name
    def show_info(self):
        print(self.name)

stu = Student("张三")
stu.show_info(stu)  # 报错：TypeError: show_info() takes 1 positional argument but 2 were given
```
### 报错原因
`self`是Python自动传递的参数，无需手动传递，手动传递会导致参数个数超出。
### 解决方案
调用实例方法时，**仅传递自定义参数**，不传递`self`。

## 避坑3：混淆类属性与实例属性的修改方式
### 错误示例
```python
class Student:
    school = "北京大学"
    def __init__(self, name):
        self.name = name

stu = Student("张三")
stu.school = "清华大学"  # 实际是添加实例属性，不是修改类属性
print(Student.school)  # 输出：北京大学（类属性未改变）
```
### 报错原因
通过对象无法修改类属性，只能为对象添加同名实例属性。
### 解决方案
修改类属性**必须通过类名**（`Student.school = "清华大学"`）。

## 避坑4：创建对象时遗漏`__init__`的自定义参数
### 错误示例
```python
class Student:
    def __init__(self, name, age):  # 需传递name和age
        self.name = name
        self.age = age

stu = Student()  # 报错：TypeError: __init__() missing 2 required positional arguments: 'name' and 'age'
```
### 报错原因
`__init__`定义了自定义参数（无默认值），创建对象时必须传递对应参数，Python自动将参数传递给`__init__`。
### 解决方案
创建对象时，**按`__init__`的定义传递所有必选参数**（`stu = Student("张三", 20)`）。

## 避坑5：`__init__`方法写了返回值
### 错误示例
```python
class Student:
    def __init__(self, name):
        self.name = name
        return "初始化成功"  # 写了返回值

stu = Student("张三")  # 报错：TypeError: __init__() should return None, not 'str'
```
### 报错原因
`__init__`是构造函数，作用是初始化属性，**不允许有返回值**（默认返回None）。
### 解决方案
删除`__init__`中的`return`语句。

# 九、核心总结
本节我们学习了Python面向对象编程的**基石——类与对象**，完成了从“面向过程”到“面向对象”的思维转变，掌握了面向对象最基础、最核心的语法，核心要点回顾：
1. **思维转变**：面向过程以“步骤”为中心，数据与方法分离；面向对象以“对象”为中心，将数据（属性）和方法（行为）封装为整体，适合复杂项目开发；
2. **类与对象**：类是事物的通用模板（抽象），对象是类的具体实例（具体）；一个类可创建无数个独立对象；
3. **核心语法**：
   - 定义类：`class 类名(大驼峰)`，类的内容包含属性和方法；
   - 创建对象：`对象名 = 类名()`，称为类的实例化，每个对象有唯一内存地址；
4. **实例方法与self**：
   - 实例方法是类中的函数，第一个参数必须是`self`，调用时无需手动传递；
   - `self`是当前对象的引用，通过`self`可访问/修改对象的属性、调用其他实例方法；
5. **构造函数`__init__`**：创建对象时自动调用，用于标准化初始化实例属性，可设置默认值，无返回值；
6. **属性区分**：
   - 实例属性：定义在`__init__`中，属于单个对象，相互独立，仅能通过对象访问；
   - 类属性：定义在类内部、方法之外，属于类本身，所有对象共享，通过类名访问/修改；
7. **核心规范**：类名遵循大驼峰命名法，实例方法必须带`self`，修改类属性必须通过类名。

至此，你已掌握面向对象的基础语法，能独立编写简单的类、创建对象、调用方法。下一节，我们将学习面向对象的**第一个核心特性——封装**，通过私有属性、私有方法、`@property`装饰器，实现数据的隐藏与可控访问，让你的面向对象代码更安全、更规范。

# 十、专栏订阅
> - <font color=black size=3>==专栏优点？==《Python从入门到实战》，专栏内容涵盖：Python基础到高级编程、Web开发（Django/Flask框架）、数据库（MySQL/ORM）、网络爬虫、Linux部署运维等全栈核心知识，以项目驱动教学，构建清晰学习路径，适合零基础入门和进阶提升的同学，跟着一步步从入门到精通！专栏地址：<https://blog.csdn.net/zsh_1314520/category_13108073.html></font>
> - <font color=black size=3>==文章是永久吗？== 一次订阅后，专栏内所有文章可永久免费查看，后续会持续更新全栈相关内容，第一时间获取最新教程！</font>
> - <font color=black size=3>==有答疑交流群吗？== 订阅专栏后有专属的全栈学习答疑群，群内提供专业问题答疑、和众多学习者抱团取暖，一起沉淀技术、赋能成长！</font>
> - <font color=black size=3>==进群方式？== 订阅专栏后可直接在专栏内申请加入答疑群，或私信博主沟通进群事宜：<https://bbs.csdn.net/topics/620104702></font>
> - <font color=black size=3>==更多干货？== 点赞+收藏+关注博主不迷路！博主博客链接：<https://blog.csdn.net/zsh_1314520?spm=1000.2115.3001.5343>，专注Python全栈技术分享，评论区留言问题会一一回复，助力大家轻松搞定Python全栈！</font>

【原创声明】

除本文原文地址以外，如发现同款内容皆为盗版，本文已收录于《[Python全栈：从入门到实战](https://blog.csdn.net/zsh_1314520/category_13108073.html)》 ，请勿购买盗版文章和专栏，如购买盗版内容不提供任何服务。原文地址：<https://blog.csdn.net/zsh_1314520/article/details/159250546>
