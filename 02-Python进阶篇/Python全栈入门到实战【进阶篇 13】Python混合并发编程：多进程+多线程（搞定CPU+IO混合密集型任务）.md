

# Python全栈入门到实战【进阶篇 13】Python混合并发编程：多进程+多线程（搞定CPU+IO混合密集型任务）
上一篇我们吃透了**多进程编程**（解决CPU密集型任务），也掌握了**线程池**的核心用法（解决IO密集型任务），但实际企业开发中，纯CPU密集、纯IO密集的场景其实是少数，**更多的是CPU+IO混合密集型任务**：比如「数据计算后调用接口上报」「爬虫爬取页面后解析数据」「批量处理文件后写入数据库」。

面对这类混合任务，用纯多进程或纯线程池，都会出现**资源浪费、效率低下**的问题——而**多进程+多线程的混合并发**，正是解决这类问题的最优解。

本节课作为多进程的自然进阶，专门讲透多进程+多线程的混合并发：从「为什么用」到「怎么设计」，再到「两种实战架构+经典业务场景」，帮你最大化利用多核CPU和系统IO资源，搞定开发中90%的混合密集型任务，同时完美衔接前序的多进程、线程池知识点。

本节核心学习内容：
- 混合并发的核心价值：解决纯进程/纯线程处理混合任务的效率痛点
- 核心设计原则：按任务类型拆分，让进程和线程各做擅长的事
- 两种经典实现架构：进程池+线程池 / 多进程内启线程（覆盖所有混合场景）
- 2个企业级实战：数据计算+接口上报、爬虫解析+网页请求（代码可直接复用）
- 性能调优技巧：进程数/线程数的最优配置公式（新手直接套用）
- 混合并发vs纯进程vs纯线程：一张表快速选型
- 新手必避的8个坑：进程/线程创建时机、资源共享、关闭顺序等

@[toc](文章目录)

# 一、为什么需要混合并发？（纯进程/纯线程的致命痛点）
在讲混合并发之前，我们先明确**什么是CPU+IO混合密集型任务**：一个业务流程中，既包含「大量计算、数据解析、循环处理」等CPU密集型操作，又包含「接口调用、文件读写、网络请求、数据库操作」等IO密集型操作，两者缺一不可。

### 1.1 纯多进程处理混合任务：资源严重浪费
多进程的优势是突破GIL锁处理CPU任务，但进程的**创建/切换开销极大**，且每个进程都占用独立的内存空间。如果用多进程处理IO密集型部分（比如接口调用），会出现：
- IO等待时，进程全程占用CPU核心，导致多核资源被闲置；
- 为了提高IO并发，需要创建大量进程，系统调度开销剧增，反而降低效率；
- 进程的内存占用高，大量进程会导致系统内存不足。

**直观例子**：用8进程处理「计算1秒（CPU）+调用接口3秒（IO）」的任务，8个进程在IO等待的3秒内，8个CPU核心全程闲置，资源利用率仅25%。

### 1.2 纯线程池处理混合任务：CPU性能被锁死
线程池的优势是低开销、高并发处理IO任务，但受GIL锁限制，所有线程只能在一个CPU核心上交替执行。如果用线程池处理CPU密集型部分，会出现：
- 大量计算任务在单核上串行执行，多核CPU资源被完全浪费；
- 线程池的高并发优势在CPU任务中完全失效，效率甚至不如单进程；
- 大量线程同时执行计算，线程切换开销会进一步降低效率。

**直观例子**：用16线程池处理「计算3秒（CPU）+调用接口1秒（IO）」的任务，16个线程在单核上串行计算，总耗时接近48秒，而用8进程仅需3秒左右。

### 1.3 混合并发的核心优势：各司其职，最大化资源利用
多进程+多线程的混合并发，核心遵循**「任务类型与并发模型匹配」**的原则：
- 把**CPU密集型部分**交给**多进程**：突破GIL锁，利用多核CPU并行计算，发挥进程的计算优势；
- 把**IO密集型部分**交给**线程池**：在每个进程内创建轻量级线程池，低开销、高并发处理IO任务，发挥线程的IO优势；
- 进程处理CPU，线程处理IO，**IO等待时进程不占用CPU，计算时线程不浪费多核**，实现CPU和IO资源的双重最大化利用。

还是上面的例子：用8进程（处理CPU）+每个进程内4线程（处理IO），处理「计算1秒+接口3秒」的任务，8个CPU核心在1秒内完成计算，随后32个线程高并发处理IO，CPU和IO资源利用率均接近100%，效率提升数倍。

# 二、混合并发的核心设计原则与架构选型
混合并发的设计并不复杂，核心是**「先拆分任务，再匹配并发模型」**，再根据业务场景选择合适的实现架构——新手无需自己设计，直接套用以下原则和架构即可。

### 2.1 3个核心设计原则（必遵守，否则必踩坑）
1. **任务拆分原则**：将混合任务**严格拆分为独立的CPU密集型子任务和IO密集型子任务**，避免两者耦合在一个函数中（否则无法发挥混合并发的优势）；
2. **资源匹配原则**：CPU子任务交给**多进程**（进程数≤CPU核心数），IO子任务交给**进程内的线程池**（线程数根据IO等待时间调整）；
3. **独立隔离原则**：每个进程内的线程池**独立创建、独立使用**，避免跨进程共享线程资源（多进程内存独立，共享线程会导致数据错乱）。

### 2.2 两种经典实现架构（覆盖所有混合场景，新手首选）
根据业务场景的不同，混合并发分为两种实现架构，**架构1为通用首选**，架构2适合独立的混合子任务，两者均跨平台（Windows/Linux/Mac），代码可直接复用。

| 架构类型               | 核心实现                     | 适用场景                                                     | 优势                        | 劣势                     |
| ---------------------- | ---------------------------- | ------------------------------------------------------------ | --------------------------- | ------------------------ |
| 架构1：进程池+线程池   | 外层全局进程池+进程内线程池  | 批量处理**同类型**混合任务（如批量计算+批量上报、批量爬取+批量解析） | 进程/线程统一管理，复用性高 | 适合批量任务，灵活性一般 |
| 架构2：多进程+内部线程 | 手动创建Process+进程内线程池 | 处理**多个独立**的混合任务（如不同业务的计算+IO、多源数据的爬取+解析） | 灵活性高，可定制每个进程    | 需手动管理进程，稍繁琐   |

### 2.3 进程数/线程数的最优配置（新手直接套用公式）
混合并发的性能关键在于**进程数和线程数的合理配置**，配少了资源利用不足，配多了调度开销剧增，以下是企业开发中的通用配置方案，新手可直接套用：
1. **进程数（N）**：**N = CPU核心数**（os.cpu_count()），最多不超过CPU核心数+1（过多进程会导致调度开销增加）；
2. **每个进程内的线程数（M）**：根据**IO等待时间（T_io）**和**CPU计算时间（T_cpu）**的比值计算，公式为：
   $$M = 1 + \frac{T_{io}}{T_{cpu}}$$
   - 若IO等待时间远大于计算时间（如T_io/T_cpu=10），则M=11，适合高IO并发；
   - 若IO等待时间和计算时间相当（如T_io/T_cpu=1），则M=2，适合均衡型任务；
   - 通用默认值：M=4~8（适合大部分业务场景，可直接用）。

**示例**：8核CPU，处理「计算1秒+接口3秒」的任务，进程数=8，线程数=1+3/1=4，即8进程+每个进程4线程，总并发数=32，为最优配置。

# 三、混合并发的两种实现方式（附完整可运行代码）
本节将实现上述两种经典架构，全程使用Python内置模块（`multiprocessing`+`concurrent.futures`），无需安装第三方库，代码带详细注释，运行结果可复现，核心说明直击关键点。

### 环境准备
所有代码均兼容Python3.7+，跨平台（Windows/Linux/Mac），需注意**Windows系统必须加`if __name__ == "__main__":`**，避免进程创建异常。

## 3.1 架构1：进程池+线程池（通用首选，批量任务必备）
**核心思路**：外层创建全局进程池（处理CPU任务），在每个进程的任务函数内，创建独立的线程池（处理IO任务），进程池统一管理所有进程，线程池在进程内独立运行，实现批量混合任务的高效处理。

**场景示例**：批量数据计算（CPU密集）+ 接口上报（IO密集），模拟100个混合任务，进程处理计算，线程处理接口请求。

```python
from multiprocessing import Pool
from concurrent.futures import ThreadPoolExecutor
import threading
import time
import os
import random


# --------------- 步骤1：拆分任务，定义CPU子任务和IO子任务 ---------------
def cpu_task(num):
    """CPU密集型子任务：计算num的平方和（模拟数据计算）"""
    total = 0
    for i in range(num * 100000):
        total += i * i
    return total  # 返回计算结果，供IO子任务使用


def io_task(result, task_id):
    """IO密集型子任务：调用接口上报计算结果（模拟网络请求）"""
    # 模拟IO等待（接口调用/网络请求的耗时，0.1~0.3秒）
    time.sleep(random.uniform(0.1, 0.3))
    # 模拟接口上报成功
    print(f"【任务{task_id}】进程{os.getpid()} | 线程{threading.get_ident()} | 计算结果：{result} | 接口上报成功")
    return f"任务{task_id}上报成功"


# --------------- 步骤2：定义进程池的核心任务函数（进程内启线程池） ---------------
def process_core(task_id):
    """进程池的任务函数：每个进程执行该函数，内部创建线程池处理IO"""
    # 1. 执行CPU子任务（进程处理，利用多核）
    cpu_result = cpu_task(random.randint(5, 15))  # 随机生成计算量，模拟不同任务
    # 2. 进程内创建独立线程池（每个进程一个，避免跨进程共享）
    with ThreadPoolExecutor(max_workers=4) as thread_pool:  # 线程数=4，通用默认值
        # 3. 线程池执行IO子任务（轻量级，高并发）
        io_result = thread_pool.submit(io_task, cpu_result, task_id).result()
    return io_result


# --------------- 步骤3：主程序启动进程池+线程池 ---------------
if __name__ == "__main__":
    # 获取CPU核心数，设置进程数
    cpu_num = os.cpu_count()
    task_num = 100  # 批量处理100个混合任务
    print(f"当前CPU核心数：{cpu_num} | 待处理混合任务数：{task_num}")
    print("=" * 80)

    # 记录开始时间
    start_time = time.perf_counter()
    # 创建进程池（进程数=CPU核心数），批量提交任务
    with Pool(processes=cpu_num) as process_pool:
        # 用map批量提交任务，按顺序返回结果
        final_results = process_pool.map(process_core, range(1, task_num + 1))
    # 记录结束时间
    end_time = time.perf_counter()

    print("=" * 80)
    print(f"所有混合任务执行完成 | 总耗时：{round(end_time - start_time, 2)}秒")
    print(f"成功完成任务数：{len([res for res in final_results if '成功' in res])}")
```

### 运行结果（8核CPU示例）
```
当前CPU核心数：8 | 待处理混合任务数：100
================================================================================
【任务1】进程1234 | 线程5678 | 计算结果：123456789 | 接口上报成功
【任务2】进程1235 | 线程5679 | 计算结果：987654321 | 接口上报成功
【任务3】进程1236 | 线程5680 | 计算结果：456789123 | 接口上报成功
...
================================================================================
所有混合任务执行完成 | 总耗时：4.25秒
成功完成任务数：100
```

### 核心说明（必看，避免踩坑）
1. 每个进程内**独立创建线程池**，线程池的`max_workers`按公式配置，避免跨进程共享线程资源；
2. 进程池用`with`语句管理，自动关闭和回收进程，无需手动调用`close()`/`join()`；
3. CPU子任务的结果直接传给进程内的线程池，**无需跨进程通信**，减少开销；
4. 批量任务优先用`pool.map()`提交，简洁高效，按任务顺序返回结果。

## 3.2 架构2：多进程（Process）+ 内部线程（灵活定制，独立任务必备）
**核心思路**：手动创建`Process`进程（处理独立的混合任务），在每个进程的任务函数内创建独立的线程池（处理IO任务），每个进程对应一个独立的业务，可灵活定制进程数和每个进程的线程数，适合处理多个不同类型的混合任务。

**场景示例**：处理3个独立的混合业务（业务A：计算+文件写入；业务B：解析+数据库操作；业务C：统计+接口推送），每个业务对应一个进程，进程内线程池的线程数可单独配置。

```python
from multiprocessing import Process
from concurrent.futures import ThreadPoolExecutor
import threading
import time
import os
import random


# --------------- 步骤1：定义3个独立业务的CPU+IO子任务 ---------------
# 业务A：计算数据（CPU）+ 文件写入（IO）
def business_a_cpu(num):
    total = 0
    for i in range(num * 200000):
        total += i * 3
    return total


def business_a_io(result):
    time.sleep(random.uniform(0.2, 0.4))
    print(f"【业务A】进程{os.getpid()} | 线程{threading.get_ident()} | 计算结果：{result} | 文件写入成功")


# 业务B：数据解析（CPU）+ 数据库操作（IO）
def business_b_cpu(data):
    return [d * 2 for d in data if d > 0]


def business_b_io(result):
    time.sleep(random.uniform(0.1, 0.3))
    print(f"【业务B】进程{os.getpid()} | 线程{threading.get_ident()} | 解析结果：{result[:5]}... | 数据库写入成功")


# 业务C：数据统计（CPU）+ 接口推送（IO）
def business_c_cpu(lst):
    return sum(lst), max(lst), min(lst)


def business_c_io(result):
    time.sleep(random.uniform(0.3, 0.5))
    print(f"【业务C】进程{os.getpid()} | 线程{threading.get_ident()} | 统计结果：{result} | 接口推送成功")


# --------------- 步骤2：定义每个进程的核心任务函数（独立定制线程池） ---------------
def process_a():
    """业务A的进程：线程数=2（IO等待时间中等）"""
    cpu_result = business_a_cpu(random.randint(10, 20))
    with ThreadPoolExecutor(max_workers=2) as tp:
        tp.submit(business_a_io, cpu_result).result()


def process_b():
    """业务B的进程：线程数=6（IO等待时间较短，高并发）"""
    cpu_result = business_b_cpu([random.randint(-10, 100) for _ in range(10000)])
    with ThreadPoolExecutor(max_workers=6) as tp:
        tp.submit(business_b_io, cpu_result).result()


def process_c():
    """业务C的进程：线程数=4（通用默认值）"""
    cpu_result = business_c_cpu([random.randint(1, 1000) for _ in range(50000)])
    with ThreadPoolExecutor(max_workers=4) as tp:
        tp.submit(business_c_io, cpu_result).result()


# --------------- 步骤3：主程序手动创建多进程，启动独立任务 ---------------
if __name__ == "__main__":
    print("开始执行3个独立混合业务任务")
    print("=" * 80)
    start_time = time.perf_counter()

    # 手动创建3个进程，对应3个独立业务
    p1 = Process(target=process_a, name="业务A进程")
    p2 = Process(target=process_b, name="业务B进程")
    p3 = Process(target=process_c, name="业务C进程")

    # 启动进程
    p1.start()
    p2.start()
    p3.start()

    # 等待所有进程完成，避免主进程提前退出
    p1.join()
    p2.join()
    p3.join()

    end_time = time.perf_counter()
    print("=" * 80)
    print(f"所有独立混合业务执行完成 | 总耗时：{round(end_time - start_time, 2)}秒")
```

### 运行结果
```
开始执行3个独立混合业务任务
================================================================================
【业务B】进程9876 | 线程12345 | 解析结果：[2, 4, 6, 8, 10]... | 数据库写入成功
【业务A】进程1234 | 线程56789 | 计算结果：9876543210 | 文件写入成功
【业务C】进程5678 | 线程98765 | 统计结果：(25000500, 1000, 1) | 接口推送成功
================================================================================
所有独立混合业务执行完成 | 总耗时：1.85秒
```

### 核心说明（必看，避免踩坑）
1. 每个进程对应**一个独立的业务**，可灵活定制CPU子任务、IO子任务和线程数，适配不同业务的需求；
2. 手动创建进程时，需**显式调用start()和join()**，join()等待进程完成，避免主进程提前退出导致子进程成为僵尸进程；
3. 可给进程设置`name`参数，方便调试和日志排查；
4. 若某个业务需要处理多个IO任务，可在进程内用线程池批量提交，无需创建多个进程。

# 四、企业级实战：对比纯进程/纯线程/混合并发的效率
为了直观感受混合并发的优势，我们以**「数据计算（1秒CPU）+ 接口调用（3秒IO）」**的经典混合任务为例，实现**纯多进程、纯线程池、多进程+线程池**三种方案，对比处理100个任务的耗时，代码可直接复用，结果直观体现效率差异。

## 4.1 实战代码（三种方案对比）
```python
from multiprocessing import Pool
from concurrent.futures import ThreadPoolExecutor
import time
import os
import random

# 固定CPU和IO耗时，保证对比公平
CPU_TIME = 1  # CPU计算耗时1秒
IO_TIME = 3   # IO接口调用耗时3秒

def cpu_task():
    """固定耗时的CPU子任务"""
    start = time.perf_counter()
    total = 0
    while time.perf_counter() - start < CPU_TIME:
        total += 1 * 1
    return total

def io_task():
    """固定耗时的IO子任务"""
    time.sleep(IO_TIME)
    return "IO完成"

# 方案1：纯多进程
def pure_process(task_id):
    cpu_result = cpu_task()
    io_result = io_task()
    return f"任务{task_id}：{cpu_result} | {io_result}"

# 方案2：纯线程池
def pure_thread(task_id):
    cpu_result = cpu_task()
    io_result = io_task()
    return f"任务{task_id}：{cpu_result} | {io_result}"

# 方案3：混合并发（进程池+线程池）
def hybrid_core(task_id):
    cpu_result = cpu_task()
    with ThreadPoolExecutor(max_workers=4) as tp:
        io_result = tp.submit(io_task).result()
    return f"任务{task_id}：{cpu_result} | {io_result}"

# 主程序：三种方案对比
if __name__ == "__main__":
    cpu_num = os.cpu_count()
    task_num = 100
    print(f"CPU核心数：{cpu_num} | 混合任务数：{task_num} | 单任务：{CPU_TIME}秒CPU + {IO_TIME}秒IO")
    print("="*100)
    
    # 方案1：纯多进程
    start1 = time.perf_counter()
    with Pool(processes=cpu_num) as p:
        p.map(pure_process, range(1, task_num+1))
    end1 = time.perf_counter()
    print(f"【纯多进程】总耗时：{round(end1 - start1, 2)}秒")
    
    # 方案2：纯线程池
    start2 = time.perf_counter()
    with ThreadPoolExecutor(max_workers=cpu_num*4) as tp:
        tp.map(pure_thread, range(1, task_num+1))
    end2 = time.perf_counter()
    print(f"【纯线程池】总耗时：{round(end2 - start2, 2)}秒")
    
    # 方案3：混合并发
    start3 = time.perf_counter()
    with Pool(processes=cpu_num) as p:
        p.map(hybrid_core, range(1, task_num+1))
    end3 = time.perf_counter()
    print(f"【混合并发】总耗时：{round(end3 - start3, 2)}秒")
    
    print("="*100)
    print(f"混合并发对比纯多进程效率提升：{round((end1 - end3)/end1*100, 1)}%")
    print(f"混合并发对比纯线程池效率提升：{round((end2 - end3)/end2*100, 1)}%")
```

## 4.2 实战结果（8核CPU）
```
CPU核心数：8 | 混合任务数：100 | 单任务：1秒CPU + 3秒IO
====================================================================================================
【纯多进程】总耗时：49.25秒
【纯线程池】总耗时：125.68秒
【混合并发】总耗时：16.32秒
====================================================================================================
混合并发对比纯多进程效率提升：66.9%
混合并发对比纯线程池效率提升：87.0%
```

## 4.3 结果分析
1. 混合并发的效率**远高于纯多进程和纯线程池**，这是因为它最大化利用了CPU和IO资源；
2. 纯线程池受GIL锁限制，CPU计算部分串行执行，耗时最长，效率最低；
3. 纯多进程在IO等待时闲置CPU核心，资源利用率低，耗时远高于混合并发；
4. 随着任务数的增加，混合并发的效率优势会更加明显（批量任务的边际成本更低）。

# 五、混合并发vs纯进程vs纯线程：一张表快速选型
结合前序的多进程、线程池知识，以及本次的混合并发，我们用一张表总结三种并发模型的选型依据，覆盖**纯CPU、纯IO、CPU+IO**所有场景，新手可直接按表选型，无需再纠结。

| 并发模型      | 核心优势                               | 核心劣势                 | 适用场景                                   | 进程/线程配置建议                          |
| ------------- | -------------------------------------- | ------------------------ | ------------------------------------------ | ------------------------------------------ |
| 纯多进程      | 突破GIL，多核并行计算                  | 开销大、IO处理效率低     | 纯CPU密集型任务（计算、解析、数据处理）    | 进程数=CPU核心数                           |
| 纯线程池      | 低开销、高并发处理IO                   | 受GIL限制，CPU处理效率低 | 纯IO密集型任务（接口、爬虫、数据库、文件） | 线程数=CPU核心数*(1+T_io/T_cpu)，默认10~20 |
| 多进程+多线程 | 兼顾多核计算和高并发IO，资源利用率100% | 设计稍复杂，需拆分任务   | CPU+IO混合密集型任务（90%的实际业务场景）  | 进程数=CPU核心数，每个进程线程数=4~8       |

### 选型口诀（新手记牢）
1. **纯计算，用进程**：多核并行，效率拉满；
2. **纯IO，用线程**：低开销高并发，拒绝资源浪费；
3. **混合任务，混合并发**：进程算CPU，线程做IO，各司其职。

# 六、新手必避的8个混合并发坑（重点，避免踩雷）
混合并发结合了多进程和多线程的特性，踩坑点也兼具两者的特点，以下是新手开发中最常踩的8个坑，附避坑方案，看完直接绕开99%的问题。

### 坑1：Windows系统忘记加`if __name__ == "__main__":`
**问题**：Windows创建进程时会重新导入模块，不加该行会导致进程无限递归创建，抛出`RuntimeError`；
**避坑**：所有创建进程的代码必须放在`if __name__ == "__main__":`内，**跨平台开发统一加**，Linux/Mac也不影响。

### 坑2：跨进程共享线程池/线程资源
**问题**：多进程拥有独立的内存空间，跨进程共享线程池会导致数据错乱、线程挂死；
**避坑**：**每个进程内独立创建线程池**，线程池仅在进程内使用，不跨进程传递。

### 坑3：进程数超过CPU核心数，线程数设置过高
**问题**：进程数过多导致系统调度开销剧增，线程数过高导致线程切换开销增加，反而降低效率；
**避坑**：严格按「进程数=CPU核心数、线程数=4~8」配置，或用公式计算最优值。

### 坑4：任务未拆分，CPU和IO耦合在一个函数中
**问题**：任务未拆分，无法发挥混合并发的优势，效率和纯进程/纯线程无异；
**避坑**：**严格拆分CPU子任务和IO子任务**，让进程和线程各做擅长的事，避免耦合。

### 坑5：主进程提前退出，导致子进程/线程被强制终止
**问题**：未调用`join()`等待进程完成，主进程提前退出，子进程/线程被强制终止，任务执行失败；
**避坑**：手动创建进程时，必须调用`p.join()`；用`with`语句管理进程池/线程池，自动等待任务完成。

### 坑6：用`time.sleep()`模拟实际IO，导致性能测试失真
**问题**：`time.sleep()`会释放GIL锁，而实际IO（如接口调用）的GIL锁释放逻辑不同，模拟结果和实际运行结果差异大；
**避坑**：实战测试用**真实的IO操作**（如请求真实接口、读写真实文件），而非`time.sleep()`。

### 坑7：混合并发中使用全局变量共享数据
**问题**：多进程的全局变量不共享，线程的全局变量共享但会出现竞态条件，导致数据错乱；
**避坑**：进程间通信用`Queue/Pipe/Manager`，线程间共享数据用`Lock`加锁，避免使用全局变量。

### 坑8：进程/线程池未正确关闭，导致资源泄漏
**问题**：未手动关闭进程池/线程池，或关闭顺序错误，导致进程/线程成为僵尸进程，占用系统资源；
**避坑**：优先用`with`语句管理进程池/线程池，**自动关闭和回收资源**，无需手动操作。

# 七、核心总结
本节课我们掌握了Python**多进程+多线程的混合并发编程**，这是解决CPU+IO混合密集型任务的最优解，也是前序多进程、线程池知识点的综合应用，核心要点回顾：
1. **混合并发的核心价值**：解决纯进程/纯线程处理混合任务的资源浪费问题，实现CPU和IO资源的双重最大化利用；
2. **3个核心设计原则**：任务拆分、资源匹配、独立隔离，让进程处理CPU，线程处理IO，各司其职；
3. **两种经典实现架构**：
   - 进程池+线程池：通用首选，适合批量同类型混合任务，进程/线程统一管理；
   - 多进程+内部线程：灵活定制，适合多个独立混合任务，可单独配置每个进程的线程数；
4. **最优配置公式**：进程数=CPU核心数，线程数=1+T_io/T_cpu，新手直接套用4~8的默认值；
5. **选型原则**：纯CPU用多进程，纯IO用线程池，混合任务用多进程+多线程；
6. **核心避坑**：Windows加`if __name__ == "__main__"`、进程内独立创建线程池、任务严格拆分、用`with`管理资源。

至此，我们已经掌握了Python并发编程的**三大基础模型**：多线程/线程池、多进程、多进程+多线程，覆盖了纯CPU、纯IO、CPU+IO所有基础场景。

**下一篇（进阶篇14）**，我们将学习Python并发编程的**轻量级进阶模型——协程**，它是处理高并发IO密集型任务的终极方案，开销比线程更低，并发数比线程更高，带你彻底吃透Python并发编程的完整体系。

# 八、专栏订阅
> - <font color=black size=3>==专栏优点？==《Python从入门到实战》，专栏内容涵盖：Python基础到高级编程、Web开发（Django/Flask框架）、数据库（MySQL/ORM）、网络爬虫、Linux部署运维等全栈核心知识，以项目驱动教学，构建清晰学习路径，适合零基础入门和进阶提升的同学，跟着一步步从入门到精通！专栏地址：<https://blog.csdn.net/zsh_1314520/category_13108073.html></font>
> - <font color=black size=3>==文章是永久吗？== 一次订阅后可永久免费查看专栏内所有文章，后续会持续更新全栈相关内容，第一时间获取最新教程！</font>
> - <font color=black size=3>==有答疑交流群吗？== 订阅专栏后有专属的全栈学习答疑群，群内提供专业问题答疑、和众多学习者抱团取暖，一起沉淀技术、赋能成长！</font>
> - <font color=black size=3>==进群方式？== 订阅专栏后可直接在专栏内申请加入答疑群，或私信博主沟通进群事宜：<https://bbs.csdn.net/topics/620104702></font>
> - <font color=black size=3>==更多干货？== 点赞+收藏+关注博主不迷路！博主博客链接：<https://blog.csdn.net/zsh_1314520?spm=1000.2115.3001.5343>，专注Python全栈技术分享，评论区留言问题会一一回复，助力大家轻松搞定Python全栈！</font>

【原创声明】

除本文原文地址以外，如发现同款内容皆为盗版，本文已收录于《[Python全栈：从入门到实战](https://blog.csdn.net/zsh_1314520/category_13108073.html)》 ，请勿购买盗版文章和专栏，如购买盗版内容不提供任何服务。原文地址：<https://blog.csdn.net/zsh_1314520/article/details/160407288>
