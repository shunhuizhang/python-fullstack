

# Python全栈入门到实战【数据库篇 11】MySQL DCL用户管理与权限控制详解，数据库安全核心
上一篇《数据库篇 10》中，我们已经掌握了MySQL DQL的排序查询与分页查询，完成了所有数据查询语法的学习。本篇作为数据库篇的第十一篇，我们将学习**数据库安全的核心语法**——DCL数据控制语言。DCL主要用于管理数据库用户和控制用户的访问权限，是保障数据库数据安全的重要手段，虽然开发人员日常操作较少，但在应用部署和数据库维护中是必须掌握的技能。

本文为Python全栈开发者与数据库入门者量身打造，详细讲解用户的查询、创建、修改密码和删除操作，以及权限的查询、授予和撤销方法，每一个语法都有清晰的说明和可直接运行的实战示例，同时重点标注新手最容易踩的主机名通配符和权限粒度问题，即使是完全没有数据库管理经验的同学，也能快速掌握DCL的核心用法。

本节核心学习内容：
1. DCL语言概述：作用与核心分类
2. 用户管理：查询、创建、修改密码、删除用户
3. 常用权限：MySQL核心权限说明与使用场景
4. 权限控制：查询权限、授予权限、撤销权限
5. 完整实战：从创建用户到分配权限的完整流程
6. 常见误区：主机名通配符、权限粒度、安全注意事项
7. 核心总结：DCL语法速查表，方便开发时快速查阅

# 一、DCL语言概述
DCL（Data Control Language，数据控制语言）是用于**管理数据库用户和控制用户访问权限**的SQL语言，是数据库安全体系的核心组成部分。

DCL主要包含两个核心部分：
- **用户管理**：创建、修改、删除数据库用户，管理用户的登录信息
- **权限控制**：授予或撤销用户对数据库、表、字段的访问权限

> ⚠️ 注意：这类SQL开发人员日常操作的比较少，主要是DBA（Database Administrator，数据库管理员）使用。但作为全栈开发者，必须掌握基本的用户和权限管理，在部署应用时创建专用的数据库用户，避免使用root用户直接连接数据库，保障数据安全。

# 二、用户管理
MySQL的用户信息存储在系统数据库`mysql`的`user`表中，所有用户管理操作都是对这个表的操作。

## 2.1 查询用户
首先需要切换到`mysql`系统数据库，然后查询`user`表即可查看所有用户信息。

**语法格式**：
```sql
USE mysql;
SELECT * FROM user;
```

**查询结果说明**：
- `User`字段：用户名
- `Host`字段：允许登录的主机地址
- `authentication_string`字段：加密后的用户密码

## 2.2 创建用户
创建一个新的数据库用户，并指定其允许登录的主机地址和登录密码。

**语法格式**：
```sql
CREATE USER '用户名'@'主机名' IDENTIFIED BY '密码';
```

**主机名说明**：
- `'localhost'`：只允许在本地登录
- `'%'`：允许在任意主机登录（通配符）
- `'192.168.1.%'`：允许在192.168.1网段的所有主机登录
- `'192.168.1.100'`：只允许在指定IP的主机登录

**实战示例**：
```sql
-- 创建一个只能在本地登录的用户，用户名：zhangsan，密码：123456
CREATE USER 'zhangsan'@'localhost' IDENTIFIED BY '123456';

-- 创建一个可以在任意主机登录的用户，用户名：lisi，密码：123456
CREATE USER 'lisi'@'%' IDENTIFIED BY '123456';
```

## 2.3 修改用户密码
修改指定用户的登录密码，MySQL 8.0及以上版本推荐使用以下语法：

**语法格式**：
```sql
ALTER USER '用户名'@'主机名' IDENTIFIED WITH mysql_native_password BY '新密码';
```

**实战示例**：
```sql
-- 修改zhangsan用户的密码为654321
ALTER USER 'zhangsan'@'localhost' IDENTIFIED WITH mysql_native_password BY '654321';
```

## 2.4 删除用户
删除指定的数据库用户。

**语法格式**：
```sql
DROP USER '用户名'@'主机名';
```

**实战示例**：
```sql
-- 删除只能本地登录的zhangsan用户
DROP USER 'zhangsan'@'localhost';

-- 删除可以任意主机登录的lisi用户
DROP USER 'lisi'@'%';
```

> 🚨 重要注意：用户名和主机名是一个整体，必须同时指定才能唯一标识一个用户。例如`'zhangsan'@'localhost'`和`'zhangsan'@'%'`是两个完全不同的用户。

# 三、常用权限说明
MySQL中定义了几十种权限，覆盖了数据库操作的方方面面，但日常开发中最常用的只有以下几种：

| 权限                  | 说明               |
| --------------------- | ------------------ |
| `ALL, ALL PRIVILEGES` | 所有权限           |
| `SELECT`              | 查询数据           |
| `INSERT`              | 插入数据           |
| `UPDATE`              | 修改数据           |
| `DELETE`              | 删除数据           |
| `ALTER`               | 修改表结构         |
| `DROP`                | 删除数据库/表/视图 |
| `CREATE`              | 创建数据库/表      |

# 四、权限控制
权限控制是DCL的核心，通过授予和撤销权限，可以精确控制每个用户对数据库的操作范围。

## 4.1 查询权限
查询指定用户拥有的所有权限。

**语法格式**：
```sql
SHOW GRANTS FOR '用户名'@'主机名';
```

**实战示例**：
```sql
-- 查询lisi用户的权限
SHOW GRANTS FOR 'lisi'@'%';
```

## 4.2 授予权限
给指定用户授予一个或多个权限。

**语法格式**：
```sql
GRANT 权限列表 ON 数据库名.表名 TO '用户名'@'主机名';
```

**注意事项**：
- 多个权限之间，使用逗号分隔
- 授权时，数据库名和表名可以使用`*`进行通配，代表所有
- `*.*`代表所有数据库的所有表
- `test.*`代表test数据库的所有表

**实战示例**：
```sql
-- 给lisi用户授予test数据库所有表的查询权限
GRANT SELECT ON test.* TO 'lisi'@'%';

-- 给lisi用户授予test数据库所有表的查询、插入、修改、删除权限
GRANT SELECT, INSERT, UPDATE, DELETE ON test.* TO 'lisi'@'%';

-- 给lisi用户授予所有数据库所有表的所有权限（谨慎使用！）
GRANT ALL ON *.* TO 'lisi'@'%';
```

## 4.3 撤销权限
撤销指定用户的一个或多个权限。

**语法格式**：
```sql
REVOKE 权限列表 ON 数据库名.表名 FROM '用户名'@'主机名';
```

**实战示例**：
```sql
-- 撤销lisi用户对test数据库所有表的删除权限
REVOKE DELETE ON test.* FROM 'lisi'@'%';

-- 撤销lisi用户对test数据库所有表的所有权限
REVOKE ALL ON test.* FROM 'lisi'@'%';
```

> 💡 注意：权限修改后，需要执行`FLUSH PRIVILEGES;`命令刷新权限，使修改立即生效。

# 五、完整实战演示
下面我们通过一个完整的案例，演示从创建用户到分配权限的完整流程。

## 5.1 需求说明
1. 创建一个名为`app_user`的用户，允许在任意主机登录，密码为`app123456`
2. 给该用户授予`test`数据库所有表的查询、插入、修改权限
3. 查询该用户的权限，确认授权成功
4. 撤销该用户的修改权限
5. 最后删除该用户

## 5.2 实现步骤
```sql
-- 1. 创建用户
CREATE USER 'app_user'@'%' IDENTIFIED BY 'app123456';

-- 2. 授予权限
GRANT SELECT, INSERT, UPDATE ON test.* TO 'app_user'@'%';

-- 3. 刷新权限
FLUSH PRIVILEGES;

-- 4. 查询权限
SHOW GRANTS FOR 'app_user'@'%';

-- 5. 撤销修改权限
REVOKE UPDATE ON test.* FROM 'app_user'@'%';

-- 6. 刷新权限
FLUSH PRIVILEGES;

-- 7. 再次查询权限，确认撤销成功
SHOW GRANTS FOR 'app_user'@'%';

-- 8. 删除用户
DROP USER 'app_user'@'%';
```

# 六、常见误区与避坑指南
1. **主机名的重要性**：用户名和主机名是一个整体，必须同时指定才能唯一标识一个用户。很多新手创建用户时只写用户名，默认主机名是`%`，导致用户可以从任意主机登录，存在安全隐患。

2. **权限粒度问题**：遵循最小权限原则，只给用户授予必要的权限，不要轻易授予`ALL`权限。例如，应用程序用户只需要查询、插入、修改权限，不需要删除和创建表的权限。

3. **root用户的使用**：绝对不要在应用程序中使用root用户连接数据库，root用户拥有所有权限，一旦泄露会导致整个数据库被控制。应该为每个应用创建专用的用户，只授予必要的权限。

4. **密码强度问题**：不要使用简单的密码，应该使用包含大小写字母、数字和特殊字符的强密码，定期更换密码。

5. **权限生效问题**：修改权限后，必须执行`FLUSH PRIVILEGES;`命令刷新权限，否则修改不会立即生效。

6. **删除用户时的注意事项**：删除用户时，该用户拥有的所有权限都会被自动撤销，不需要手动先撤销权限。

# 七、核心总结：DCL语法速查表
为了方便后续开发时快速查阅，整理了DCL用户管理与权限控制的核心语法速查表：

| 操作类型 | 语法格式                                                     | 核心要点                           |
| -------- | ------------------------------------------------------------ | ---------------------------------- |
| 查询用户 | `USE mysql; SELECT * FROM user;`                             | 用户信息存储在mysql.user表中       |
| 创建用户 | `CREATE USER '用户名'@'主机名' IDENTIFIED BY '密码';`        | 主机名用%表示任意主机              |
| 修改密码 | `ALTER USER '用户名'@'主机名' IDENTIFIED WITH mysql_native_password BY '新密码';` | MySQL 8.0推荐语法                  |
| 删除用户 | `DROP USER '用户名'@'主机名';`                               | 用户名和主机名是一个整体           |
| 查询权限 | `SHOW GRANTS FOR '用户名'@'主机名';`                         | 查看用户拥有的所有权限             |
| 授予权限 | `GRANT 权限列表 ON 数据库.表 TO '用户名'@'主机名';`          | 多个权限用逗号分隔，*表示所有      |
| 撤销权限 | `REVOKE 权限列表 ON 数据库.表 FROM '用户名'@'主机名';`       | 撤销后需要刷新权限                 |
| 刷新权限 | `FLUSH PRIVILEGES;`                                          | 权限修改后必须执行，使修改立即生效 |

# 八、专栏订阅
> - <font color=black size=3>==专栏优点？==《Python从入门到实战》，专栏内容涵盖：Python基础到高级编程、并发编程（进程/线程/协程）、网络编程（TCP/UDP/Socket）、核心内置/第三方模块、数据库核心实战、Web开发（Django/Flask/FastAPI框架）、数据库（MySQL/ORM/异步数据库）、网络爬虫（同步/异步/分布式）、AI实战、Linux部署运维等全栈核心知识，以项目驱动教学，构建清晰学习路径，适合零基础入门和进阶提升的同学，跟着一步步从入门到精通！专栏地址：<https://blog.csdn.net/zsh_1314520/category_13108073.html></font>
> - <font color=black size=3>==文章是永久吗？== 一次订阅后可永久免费查看专栏内所有文章，后续会持续更新全栈相关内容，第一时间获取最新教程！</font>
> - <font color=black size=3>==有答疑交流群吗？== 订阅专栏后有专属的全栈学习答疑群，群内提供专业问题答疑、和众多学习者抱团取暖，一起沉淀技术、赋能成长！</font>
> - <font color=black size=3>==进群方式？== 订阅专栏后可直接在专栏内申请加入答疑群，或私信博主沟通进群事宜：<https://bbs.csdn.net/topics/620104702></font>
> - <font color=black size=3>==更多干货？== 点赞+收藏+关注博主不迷路！博主博客链接：<https://blog.csdn.net/zsh_1314520?spm=1000.2115.3001.5343>，专注Python全栈技术分享，评论区留言问题会一一回复，助力大家轻松搞定Python全栈！</font>

【原创声明】

除本文原文地址以外，如发现同款内容皆为盗版，本文已收录于《[Python全栈：从入门到实战](https://blog.csdn.net/zsh_1314520/category_13108073.html)》 ，请勿购买盗版文章和专栏，如购买盗版内容不提供任何服务。原文地址：<https://blog.csdn.net/zsh_1314520/article/details/162464189>
