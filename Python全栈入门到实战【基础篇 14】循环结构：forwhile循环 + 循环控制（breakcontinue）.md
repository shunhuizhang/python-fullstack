

# Python全栈入门到实战【基础篇 14】循环结构：for/while循环 + 循环控制（break/continue）

# 前言

哈喽各位小伙伴！前面咱们学了字典、集合这些复合数据类型，也掌握了if-elif-else的分支逻辑——但实际开发中，很多场景需要“重复执行一段代码”：
- 批量处理列表里的100个用户数据，逐个验证手机号格式；
- 爬虫循环爬取10页网页数据，直到爬完所有页数；
- 定时检查文件是否存在，直到文件生成后停止；
- 遍历字典的所有键值对，批量更新数据。

这些“重复执行逻辑”的需求，靠的就是Python的**循环结构**——核心是`for`循环（遍历可迭代对象）和`while`循环（条件满足则循环），再配合`break`/`continue`控制循环流程，就能让程序自动化完成重复任务，告别“复制粘贴代码”的低效操作。

这节咱们吃透循环的核心用法：
- `for`循环：遍历列表/字典/字符串/范围，`enumerate`获取索引+元素；
- `while`循环：条件循环、死循环+终止条件；
- 循环控制：`break`（终止循环）、`continue`（跳过当前迭代）；
- 嵌套循环：循环里套循环（如二维列表遍历、九九乘法表）；
- 循环避坑：死循环、for循环修改原序列、while循环条件永不满足。

吃透循环，你就能让程序“自动化干活”，处理批量数据、重复任务再也不用手动操作～

@[TOC](文章目录)


# 一、前置引入：为什么需要循环？
没有循环的话，重复执行代码只能靠“复制粘贴”——比如验证3个手机号要写3次判断代码，验证100个就要写100次，代码冗余、维护困难，还容易出错。

循环的核心价值是：**用固定代码处理任意数量的重复任务**。不管是处理10个还是1000个数据，只要写一次核心逻辑，循环就能自动重复执行，既简洁又高效。

Python的循环分两类，适用场景清晰：
- `for`循环：适合**已知循环次数**的场景（比如遍历10个元素的列表、生成1-100的数字）；
- `while`循环：适合**未知循环次数**的场景（比如等待用户输入正确指令、直到文件生成才停止）。


# 二、for循环：遍历可迭代对象的“遍历神器”
`for`循环是Python中最常用的循环，核心是“遍历可迭代对象”（列表、字典、字符串、range、文件等），按顺序取出每个元素执行逻辑。

## 1. for循环基础语法
```python
for 变量名 in 可迭代对象:
    循环体代码（必须缩进4个空格）
# 循环结束后执行的代码（无缩进）
```
### 核心规则
- **可迭代对象**：能逐个取出元素的对象（list/dict/str/range/tuple等）；
- **变量名**：每次循环从可迭代对象中取出的“当前元素”；
- **缩进**：循环体必须缩进（4个空格），否则抛`IndentationError`（缩进错误）。

## 2. for循环的常见遍历场景
### 场景1：遍历列表/元组（最常用）
示例：遍历数字列表，计算所有元素的累加和
```python
nums = [1, 2, 3, 4, 5]
total = 0  # 初始化累加和

for num in nums:
    total += num  # 等价于 total = total + num
    print(f"当前元素：{num}，累计和：{total}")

print(f"最终累加和：{total}")
```
**运行结果**：
```
当前元素：1，累计和：1
当前元素：2，累计和：3
当前元素：3，累计和：6
当前元素：4，累计和：10
当前元素：5，累计和：15
最终累加和：15
```

### 场景2：遍历字符串（按字符遍历）
示例：遍历字符串，统计元音字母（a/e/i/o/u）的数量
```python
text = "Python is the best language"
vowels = ["a", "e", "i", "o", "u"]
count = 0

for char in text.lower():  # 转小写，统一判断
    if char in vowels:
        count += 1

print(f"字符串中元音字母数量：{count}")  # 输出：8
```

### 场景3：遍历字典（键/值/键值对）
字典的遍历需用`keys()`/`values()`/`items()`，分别获取键、值、键值对：
```python
user = {"name": "张三", "age": 20, "phone": "13812345678"}

# 遍历所有键
print("遍历键：")
for key in user.keys():
    print(f"键：{key}")

# 遍历所有值
print("\n遍历值：")
for value in user.values():
    print(f"值：{value}")

# 遍历所有键值对（最常用）
print("\n遍历键值对：")
for key, value in user.items():
    print(f"{key}：{value}")
```

### 场景4：range()生成数字序列遍历
`range(start, end, step)`生成“左闭右开”的数字序列，适合“指定次数循环”：
- `start`：起始值（默认0）；
- `end`：结束值（必传）；
- `step`：步长（默认1）。

示例：生成1-10的数字，计算偶数的和
```python
even_total = 0
# range(1, 11) → 1,2,3,...,10
for num in range(1, 11):
    if num % 2 == 0:  # 判断偶数
        even_total += num

print(f"1-10的偶数和：{even_total}")  # 输出：30
```

### 场景5：enumerate()获取索引+元素
遍历列表时，如需同时获取“索引”和“元素”，用`enumerate(可迭代对象, start=0)`：
```python
fruits = ["苹果", "香蕉", "橙子"]

# enumerate默认从0开始索引
for idx, fruit in enumerate(fruits):
    print(f"索引{idx}：{fruit}")

# 指定索引从1开始
print("\n索引从1开始：")
for idx, fruit in enumerate(fruits, start=1):
    print(f"第{idx}个水果：{fruit}")
```
**运行结果**：
```
索引0：苹果
索引1：香蕉
索引2：橙子

索引从1开始：
第1个水果：苹果
第2个水果：香蕉
第3个水果：橙子
```

## 3. for循环的else分支（可选）
`for...else`结构：**循环正常结束（未被break终止）** 时，执行else代码块（常用于“检查是否找到目标元素”）。

示例：检查列表是否包含指定元素
```python
nums = [1, 2, 3, 4, 5]
target = 6

for num in nums:
    if num == target:
        print(f"找到目标元素：{target}")
        break  # 找到后终止循环，else不执行
else:
    # 循环正常结束（未触发break），执行else
    print(f"未找到目标元素：{target}")
```
**运行结果**：
```
未找到目标元素：6
```


# 三、while循环：条件满足就循环的“条件神器”
`while`循环是“条件驱动”的循环，只要条件表达式返回`True`，就持续执行循环体，直到条件为`False`。

## 1. while循环基础语法
```python
while 条件表达式:
    循环体代码（缩进）
# 循环结束后执行的代码
```
### 核心规则
- 先判断条件：`True`执行循环体，`False`退出循环；
- 循环体内**必须有修改条件的逻辑**（如计数器+1），否则会陷入“死循环”。

## 2. while循环的常见场景
### 场景1：基础条件循环（已知终止条件）
示例：计算1-100的累加和
```python
total = 0
count = 1  # 计数器

while count <= 100:
    total += count
    count += 1  # 关键：计数器+1，否则死循环

print(f"1-100的累加和：{total}")  # 输出：5050
```

### 场景2：死循环+break终止（未知循环次数）
“死循环”是条件永远为`True`的循环，需配合`break`手动终止，适合“等待用户输入正确指令”“直到满足条件才停止”的场景。

示例：交互式输入，直到输入`quit`退出
```python
while True:  # 条件永远为True，死循环
    cmd = input("请输入指令（输入quit退出）：")
    if cmd.lower() == "quit":
        print("退出程序！")
        break  # 终止循环
    print(f"你输入的指令是：{cmd}")
```
**运行示例**：
```
请输入指令（输入quit退出）：hello
你输入的指令是：hello
请输入指令（输入quit退出）：quit
退出程序！
```

### 场景3：while...else分支
和`for...else`逻辑一致：循环正常结束（未被break终止）时执行else代码块。

示例：猜数字游戏（猜对终止，猜错5次退出）
```python
import random
target_num = random.randint(1, 10)  # 随机生成1-10的数字
guess_count = 0

while guess_count < 5:
    guess = int(input("请猜1-10之间的数字："))
    guess_count += 1
    if guess == target_num:
        print(f"恭喜！{guess_count}次猜对了，数字是{target_num}")
        break
    elif guess > target_num:
        print("猜大了！")
    else:
        print("猜小了！")
else:
    # 循环正常结束（5次都没猜对）
    print(f"5次都没猜对，正确数字是{target_num}")
```

## 3. for vs while：怎么选？
| 特性         | for循环                  | while循环                |
| ------------ | ------------------------ | ------------------------ |
| 循环触发条件 | 遍历可迭代对象           | 条件表达式为True         |
| 循环次数     | 已知（可迭代对象长度）   | 未知（直到条件为False）  |
| 核心优势     | 遍历数据简洁             | 灵活控制循环终止         |
| 适用场景     | 批量处理列表/字典/字符串 | 等待用户输入、定时检查等 |


# 四、循环控制：break/continue 精准控制流程
`break`和`continue`是循环的“控制器”，能打破默认的循环顺序，精准控制执行逻辑（仅作用于**当前层级**的循环）。

## 1. break：终止整个循环
作用：立即终止当前循环（for/while），跳出循环体，后续迭代不再执行。

示例：遍历列表，找到第一个偶数就终止循环
```python
nums = [1, 3, 5, 4, 7, 8]

for num in nums:
    if num % 2 == 0:
        print(f"找到第一个偶数：{num}")
        break  # 终止循环，后续元素不再遍历
    print(f"当前元素：{num}（奇数）")
```
**运行结果**：
```
当前元素：1（奇数）
当前元素：3（奇数）
当前元素：5（奇数）
找到第一个偶数：4
```

## 2. continue：跳过当前迭代
作用：跳过当前循环的剩余代码，直接进入下一次迭代（不终止整个循环）。

示例：遍历1-10，只打印奇数（跳过偶数）
```python
for num in range(1, 11):
    if num % 2 == 0:
        continue  # 跳过偶数，直接进入下一次循环
    print(f"奇数：{num}")
```
**运行结果**：
```
奇数：1
奇数：3
奇数：5
奇数：7
奇数：9
```

## 3. 循环控制的注意事项
- `break`/`continue`只影响**当前层级**的循环（嵌套循环中不影响外层）；
- `continue`不要放在循环体最后一行（无意义，因为后续无代码可跳过）；
- `break`终止循环后，`for/while`的`else`分支不会执行。


# 五、嵌套循环：循环里套循环
一个循环的循环体中包含另一个循环，称为“嵌套循环”，适合处理二维数据（如二维列表、九九乘法表）。

## 1. 嵌套循环基础语法
```python
# 外层循环
for 变量1 in 可迭代对象1:
    # 内层循环
    for 变量2 in 可迭代对象2:
        内层循环体
    外层循环体
```

## 2. 示例1：遍历二维列表
二维列表是“列表中的列表”，需嵌套循环逐行、逐元素遍历：
```python
# 二维列表：3行2列
matrix = [[1, 2], [3, 4], [5, 6]]

# 外层循环遍历行
for row in matrix:
    # 内层循环遍历列
    for num in row:
        print(num, end=" ")  # end=" "：不换行
    print()  # 换行
```
**运行结果**：
```
1 2 
3 4 
5 6 
```

## 3. 示例2：打印九九乘法表（经典嵌套循环）
```python
# 外层循环：行数（1-9）
for i in range(1, 10):
    # 内层循环：列数（1-i）
    for j in range(1, i+1):
        print(f"{j}×{i}={i*j}", end="\t")  # \t：制表符，对齐
    print()  # 换行
```
**运行结果**：
```
1×1=1	
1×2=2	2×2=4	
1×3=3	2×3=6	3×3=9	
1×4=4	2×4=8	3×4=12	4×4=16	
1×5=5	2×5=10	3×5=15	4×5=20	5×5=25	
1×6=6	2×6=12	3×6=18	4×6=24	5×6=30	6×6=36	
1×7=7	2×7=14	3×7=21	4×7=28	5×7=35	6×7=42	7×7=49	
1×8=8	2×8=16	3×8=24	4×8=32	5×8=40	6×8=48	7×8=56	8×8=64	
1×9=9	2×9=18	3×9=27	4×9=36	5×9=45	6×9=54	7×9=63	8×9=72	9×9=81	
```


# 六、循环的核心避坑要点
## 坑1：死循环（最常见）
**原因**：while循环条件永远为True，或for循环遍历无限可迭代对象；
**示例（错误）**：
```python
# 死循环：count永远≤10（未修改count）
count = 1
while count <= 10:
    print(count)
    # 遗漏：count += 1
```
**解决**：while循环内必须修改条件，或加break终止：
```python
count = 1
while count <= 10:
    print(count)
    count += 1  # 关键：计数器+1
```

## 坑2：for循环遍历列表时修改原列表
**问题**：遍历列表时删除/添加元素，会导致漏遍历或无限循环；
**示例（错误）**：
```python
nums = [1, 2, 3, 4, 5]
# 遍历原列表时删除偶数，漏删4（因为列表长度变化）
for num in nums:
    if num % 2 == 0:
        nums.remove(num)
print(nums)  # 输出：[1, 3, 4, 5]（4未被删除）
```
**解决**：遍历列表的副本（`nums[:]`）：
```python
nums = [1, 2, 3, 4, 5]
# 遍历副本，修改原列表
for num in nums[:]:
    if num % 2 == 0:
        nums.remove(num)
print(nums)  # 输出：[1, 3, 5]（正确）
```

## 坑3：while循环条件永不满足
**原因**：初始条件就是False，循环体一次都不执行；
**示例（错误）**：
```python
count = 11
# 初始条件count<=10为False，循环体不执行
while count <= 10:
    print(count)
    count += 1
print("循环结束")  # 直接输出：循环结束
```
**解决**：检查条件表达式的初始值，确保符合预期。

## 坑4：缩进错误
**原因**：循环体未缩进，或缩进不一致（混合空格和Tab）；
**解决**：统一用4个空格缩进，避免混合Tab。


# 七、实战案例：批量验证手机号格式
需求：制作“手机号批量验证工具”，支持：
1. 批量验证列表中的手机号（必须是11位数字）；
2. 统计有效/无效手机号数量；
3. 输出无效手机号的索引和内容。

```python
def check_phones():
    # 待验证的手机号列表
    phones = [
        "13812345678",  # 有效
        "1398765432",   # 无效（10位）
        "137abc12345",  # 无效（含字母）
        "15098765432",  # 有效
        "188123456789"  # 无效（12位）
    ]
    
    valid_count = 0  # 有效数量
    invalid_phones = []  # 无效手机号（索引+内容）
    
    # 遍历手机号，获取索引+内容
    for idx, phone in enumerate(phones):
        # 验证：11位数字
        if phone.isdigit() and len(phone) == 11:
            valid_count += 1
        else:
            invalid_phones.append((idx, phone))
    
    # 输出结果
    print(f"===== 手机号验证结果 =====")
    print(f"总数量：{len(phones)}")
    print(f"有效数量：{valid_count}")
    print(f"无效数量：{len(invalid_phones)}")
    
    if invalid_phones:
        print("\n无效手机号列表：")
        for idx, phone in invalid_phones:
            print(f"索引{idx}：{phone}（原因：{'位数错误' if phone.isdigit() else '包含非数字字符'}）")

# 运行工具
check_phones()
```
**运行结果**：
```
===== 手机号验证结果 =====
总数量：5
有效数量：2
无效数量：3

无效手机号列表：
索引1：1398765432（原因：位数错误）
索引2：137abc12345（原因：包含非数字字符）
索引4：188123456789（原因：位数错误）
```


# 八、总结
今天咱们吃透了Python的循环结构，核心要点梳理如下：
1. **for循环**：
   - 遍历可迭代对象（列表/字典/字符串/range），适合“已知循环次数”的场景；
   - `enumerate()`可同时获取索引+元素，`for...else`判断循环是否正常结束。

2. **while循环**：
   - 条件驱动循环，适合“未知循环次数”的场景；
   - 死循环（`while True`）需配合`break`终止，避免无限执行。

3. **循环控制**：
   - `break`：终止整个循环；
   - `continue`：跳过当前迭代，进入下一次循环；
   - 仅作用于当前层级的循环。

4. **避坑要点**：
   - 避免死循环（while循环内必须修改条件）；
   - 遍历列表时修改原列表，需遍历副本；
   - 统一用4个空格缩进，避免缩进错误。

循环是Python自动化处理重复任务的核心，后续的函数、类、爬虫、数据分析都会频繁用到——建议你把实战案例扩展为“批量清洗文本数据”“循环爬取网页”等场景，加深对循环逻辑的理解～

下一节，咱们会学习**循环进阶：推导式（列表/字典/集合推导式）**：用一行代码实现循环+条件判断，大幅简化批量生成数据的代码，让程序更简洁高效～


# 九、专栏订阅
> - <font color=black size=3>==专栏优点？==《Python从入门到实战》，专栏内容涵盖：Python基础到高级编程、Web开发（Django/Flask框架）、数据库（MySQL/ORM）、网络爬虫、Linux部署运维等全栈核心知识，以项目驱动教学，构建清晰学习路径，适合零基础入门和进阶提升的同学，跟着一步步从入门到精通！专栏地址：<https://blog.csdn.net/zsh_1314520/category_13108073.html></font>
> - <font color=black size=3>==文章是永久吗？== 一次订阅后，专栏内所有文章可永久免费查看，后续会持续更新全栈相关内容，第一时间获取最新教程！</font>
> - <font color=black size=3>==有答疑交流群吗？== 订阅专栏后有专属的全栈学习答疑群，群内提供专业问题答疑、和众多学习者抱团取暖，一起沉淀技术、赋能成长！</font>
> - <font color=black size=3>==进群方式？== 订阅专栏后可直接在专栏内申请加入答疑群，或私信博主沟通进群事宜：<https://bbs.csdn.net/topics/620104702></font>
> - <font color=black size=3>==更多干货？== 点赞+收藏+关注博主不迷路！博主博客链接：<https://blog.csdn.net/zsh_1314520?spm=1000.2115.3001.5343>，专注Python全栈技术分享，评论区留言问题会一一回复，助力大家轻松搞定Python全栈！</font>

【原创声明】

除本文原文地址以外，如发现同款内容皆为盗版，本文已收录于《[Python全栈：从入门到实战](https://blog.csdn.net/zsh_1314520/category_13108073.html)》 ，请勿购买盗版文章和专栏，如购买盗版内容不提供任何服务。原文地址：<https://blog.csdn.net/zsh_1314520/article/details/157556030>
