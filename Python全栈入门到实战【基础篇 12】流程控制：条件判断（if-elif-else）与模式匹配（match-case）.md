

# Python全栈入门到实战【基础篇 12】流程控制：条件判断（if-elif-else）与模式匹配（match-case）

# 前言

哈喽各位小伙伴！前面咱们学完了字符串、编码等数据处理技巧，但这些都是“静态”的操作——实际开发中，程序需要根据不同的条件执行不同的逻辑：
- 输入分数后，自动判断“优秀/良好/及格/不及格”；
- 用户登录时，验证账号密码是否正确，正确则进入系统，错误则提示原因；
- 爬虫爬取数据时，根据响应状态码（200/404/500）执行不同的处理逻辑。

这些“根据条件做决策”的能力，靠的就是Python的**流程控制**——核心就是「条件判断（if-elif-else）」和「模式匹配（match-case）」。前者是Python最基础的分支逻辑，后者是3.10+新增的高效匹配语法，掌握这两个工具，你就能让程序“学会思考”，根据不同场景执行对应的操作～

@[TOC](文章目录)


# 一、前置引入：为什么需要流程控制？
没有流程控制的程序，只能“从头到尾”按顺序执行代码，就像一台只会按固定步骤干活的机器；而有了流程控制，程序就能像人一样“判断情况、做出选择”：
- 比如：输入1，执行“查询数据”；输入2，执行“新增数据”；输入3，执行“退出程序”；
- 比如：判断文件是否存在，存在则读取，不存在则创建；
- 比如：匹配用户输入的指令类型，字符串指令执行A逻辑，数字指令执行B逻辑。

if-elif-else是“通用型”条件判断，适合所有分支场景；match-case是“精准型”模式匹配，适合对固定格式/类型的数据做分支处理，两者互补，覆盖所有分支逻辑需求。


# 二、核心基础：if-elif-else 条件判断
if-elif-else是Python最基础的分支语句，通过“条件表达式的真假”决定执行哪一段代码，核心分为3种形式：单分支、双分支、多分支。

## 1. 单分支：满足条件才执行（if）
### 语法
```python
if 条件表达式:
    满足条件时执行的代码块（注意缩进！）
```
### 核心规则
- 条件表达式返回`True`时，执行缩进的代码块；返回`False`则跳过；
- 缩进是Python的“语法规则”，通常用4个空格（不要用Tab），缩进错误会抛`IndentationError`。

### 示例：判断是否成年
```python
age = 18
if age >= 18:
    print("你已成年，可以独立注册账号")  # 满足条件，执行该语句
print("程序结束")  # 无论条件是否满足，都会执行（无缩进）

# 条件不满足的情况
age = 17
if age >= 18:
    print("你已成年，可以独立注册账号")  # 不满足条件，跳过该语句
print("程序结束")  # 正常执行
```

**运行结果**：
```
你已成年，可以独立注册账号
程序结束
程序结束
```

## 2. 双分支：二选一执行（if-else）
### 语法
```python
if 条件表达式:
    满足条件时执行的代码块
else:
    不满足条件时执行的代码块
```
### 示例：判断登录密码是否正确
```python
input_pwd = input("请输入登录密码：")
correct_pwd = "123456"

if input_pwd == correct_pwd:
    print("密码正确，正在进入系统...")
else:
    print("密码错误，请重新输入！")
```

**运行示例1（密码正确）**：
```
请输入登录密码：123456
密码正确，正在进入系统...
```

**运行示例2（密码错误）**：
```
请输入登录密码：654321
密码错误，请重新输入！
```

## 3. 多分支：多选一执行（if-elif-else）
### 语法
```python
if 条件1:
    满足条件1执行的代码块
elif 条件2:
    不满足条件1，但满足条件2执行的代码块
elif 条件3:
    不满足条件1/2，但满足条件3执行的代码块
...
else:
    所有条件都不满足时执行的代码块
```
### 核心规则
- 按顺序判断条件，**只要满足其中一个条件，就执行对应代码块，然后跳过后续所有分支**；
- else是“兜底”分支，可选（如果没有else，所有条件不满足则无代码执行）。

### 示例：根据分数判断成绩等级
```python
score = 85
if score >= 90:
    print("成绩等级：优秀")
elif score >= 80:
    print("成绩等级：良好")  # 85≥80，执行该分支，跳过后续elif/else
elif score >= 60:
    print("成绩等级：及格")
else:
    print("成绩等级：不及格")
```

**运行结果**：
```
成绩等级：良好
```

## 4. if的进阶：条件表达式（三元运算符）
对于简单的双分支逻辑，可简化为“条件表达式”（一行代码实现），语法：
```python
结果1 if 条件表达式 else 结果2
```
含义：满足条件返回结果1，不满足返回结果2。

### 示例：简化年龄判断
```python
age = 20
result = "成年" if age >= 18 else "未成年"
print(f"年龄{age}：{result}")  # 年龄20：成年
```

## 5. 核心避坑：if语句的常见错误
### 错误1：缩进错误（IndentationError）
```python
age = 18
if age >= 18:
print("成年")  # 无缩进，抛IndentationError
```
**解决**：给条件满足的代码块加4个空格缩进。

### 错误2：条件表达式后漏写冒号（SyntaxError）
```python
age = 18
if age >= 18  # 漏写:，抛SyntaxError
    print("成年")
```
**解决**：if/elif/else后必须加冒号`:`。

### 错误3：用赋值号`=`代替判断号`==`
```python
age = 18
if age = 18:  # 赋值号代替判断号，抛SyntaxError
    print("成年")
```
**解决**：判断相等用`==`，赋值用`=`。


# 三、高效升级：match-case 模式匹配（Python3.10+）
match-case是Python 3.10版本新增的“模式匹配”语法，也叫“结构化模式匹配”，比if-elif更简洁、更适合“按固定模式匹配数据”的场景（比如匹配字面量、数据类型、数据结构）。

## 1. 为什么需要match-case？
if-elif处理“多值匹配”时，代码会冗长且可读性差：
```python
# if-elif处理多值匹配（繁琐）
cmd = "quit"
if cmd == "add":
    print("执行新增操作")
elif cmd == "delete":
    print("执行删除操作")
elif cmd == "quit":
    print("执行退出操作")
else:
    print("未知指令")
```
而match-case可直接匹配“值”，代码更简洁：
```python
cmd = "quit"
match cmd:
    case "add":
        print("执行新增操作")
    case "delete":
        print("执行删除操作")
    case "quit":
        print("执行退出操作")
    case _:  # 通配符，匹配所有未命中的情况（类似else）
        print("未知指令")
```

## 2. match-case 基础语法
```python
match 待匹配对象:
    case 模式1:
        匹配模式1时执行的代码块
    case 模式2 | 模式3:  # 多模式匹配（或）
        匹配模式2/3时执行的代码块
    case _:  # 通配符（兜底，类似else）
        未匹配任何模式时执行的代码块
```

## 3. match-case 核心用法
### 用法1：匹配字面量（字符串、数字、布尔值）
```python
# 匹配数字
num = 3
match num:
    case 1:
        print("数字1")
    case 2 | 3:  # 匹配2或3
        print("数字2或3")
    case _:
        print("其他数字")

# 匹配布尔值
flag = True
match flag:
    case True:
        print("开启功能")
    case False:
        print("关闭功能")
```

**运行结果**：
```
数字2或3
开启功能
```

### 用法2：匹配数据类型
```python
# 匹配输入数据的类型
data = "Python"
match type(data):
    case str:
        print(f"数据类型：字符串，内容：{data}")
    case int:
        print(f"数据类型：整数，值：{data}")
    case list:
        print(f"数据类型：列表，元素：{data}")
    case _:
        print("未知数据类型")
```

**运行结果**：
```
数据类型：字符串，内容：Python
```

### 用法3：匹配数据结构（列表、元组、字典）
#### 示例1：匹配列表
```python
# 匹配列表的长度和元素
lst = [1, 2]
match lst:
    case [1]:  # 匹配长度为1、元素为1的列表
        print("列表：[1]")
    case [1, 2]:  # 匹配长度为2、元素为1和2的列表
        print("列表：[1, 2]")
    case [*rest]:  # 通配符*匹配任意长度的列表，rest接收列表元素
        print(f"其他列表，元素：{rest}")
```

**运行结果**：

```
列表：[1, 2]
```

#### 示例2：匹配元组（解构赋值）

```python
# 匹配坐标元组
point = (3, 4)
match point:
    case (0, 0):
        print("坐标：原点")
    case (x, 0):
        print(f"坐标：x轴，x={x}")
    case (0, y):
        print(f"坐标：y轴，y={y}")
    case (x, y):
        print(f"坐标：({x}, {y})，非坐标轴")
```

**运行结果**：
```
坐标：(3, 4)，非坐标轴
```

### 用法4：守卫条件（给模式加额外判断）
在case后加`if`，实现“模式匹配+条件判断”的组合逻辑：
```python
# 匹配数字且判断范围
num = 15
match num:
    case n if n > 10:
        print(f"数字{n}大于10")
    case n if n > 5:
        print(f"数字{n}大于5且≤10")
    case _:
        print(f"数字{num}≤5")
```

**运行结果**：
```
数字15大于10
```

## 4. match-case 避坑要点
### 坑1：版本不兼容（Python<3.10）
match-case是3.10+新增语法，低于该版本会抛`SyntaxError`，解决：
- 升级Python到3.10及以上；
- 低版本用if-elif替代。

### 坑2：通配符`_`的位置
`_`是“兜底匹配”，必须放在所有case的最后，否则后续case不会执行：
```python
num = 1
match num:
    case _:  # 兜底匹配在最前，后续case失效
        print("其他数字")
    case 1:
        print("数字1")  # 永远不会执行
```

### 坑3：匹配字典时的注意事项
match-case匹配字典时，默认匹配“包含指定键值对”，而非完全匹配：
```python
# 匹配字典（包含name键即可）
person = {"name": "Python", "age": 30}
match person:
    case {"name": name}:
        print(f"姓名：{name}")  # 会执行，因为包含name键
```


# 四、if-elif-else vs match-case：怎么选？
| 场景                           | 推荐用法     | 原因                           |
| ------------------------------ | ------------ | ------------------------------ |
| 简单的条件判断（大小/相等）    | if-elif-else | 语法简单，无需考虑版本兼容性   |
| 多值字面量匹配（指令/状态码）  | match-case   | 代码更简洁，可读性更高         |
| 数据结构匹配（列表/元组/字典） | match-case   | 支持解构赋值，无需手动索引取值 |
| 低版本Python（<3.10）          | if-elif-else | match-case不兼容               |
| 复杂条件判断（多逻辑组合）     | if-elif-else | 条件表达式更灵活，易扩展       |


# 五、实战案例：整合流程控制的交互式工具
需求：制作一个“学生成绩管理工具”，支持以下功能：
1. 输入1：录入成绩（判断成绩是否在0-100之间）；
2. 输入2：查询成绩等级（用if-elif判断）；
3. 输入3：退出程序；
4. 输入其他值：提示“未知指令”（用match-case匹配指令）。

```python
def score_tool():
    score = None  # 初始化成绩
    while True:
        print("\n===== 学生成绩管理工具 =====")
        print("1：录入成绩")
        print("2：查询成绩等级")
        print("3：退出程序")
        cmd = input("请输入操作指令（1/2/3）：")
        
        # 用match-case匹配指令
        match cmd:
            case "1":
                # 录入成绩（条件判断合法性）
                input_score = input("请输入成绩（0-100）：")
                if not input_score.isdigit():
                    print("错误：成绩必须是数字！")
                    continue
                score = int(input_score)
                if 0 <= score <= 100:
                    print(f"成绩录入成功：{score}分")
                else:
                    print("错误：成绩必须在0-100之间！")
                    score = None
            case "2":
                # 查询成绩等级（if-elif多分支）
                if score is None:
                    print("请先录入成绩！")
                    continue
                if score >= 90:
                    level = "优秀"
                elif score >= 80:
                    level = "良好"
                elif score >= 60:
                    level = "及格"
                else:
                    level = "不及格"
                print(f"成绩：{score}分 → 等级：{level}")
            case "3":
                print("退出程序，感谢使用！")
                break
            case _:
                print("未知指令，请输入1/2/3！")

# 运行工具
score_tool()
```

**运行示例**：
```
===== 学生成绩管理工具 =====
1：录入成绩
2：查询成绩等级
3：退出程序
请输入操作指令（1/2/3）：1
请输入成绩（0-100）：88
成绩录入成功：88分

===== 学生成绩管理工具 =====
1：录入成绩
2：查询成绩等级
3：退出程序
请输入操作指令（1/2/3）：2
成绩：88分 → 等级：良好

===== 学生成绩管理工具 =====
1：录入成绩
2：查询成绩等级
3：退出程序
请输入操作指令（1/2/3）：3
退出程序，感谢使用！
```


# 六、总结
今天咱们吃透了Python的两大流程控制工具，核心要点梳理如下：
1. **if-elif-else**：
   - 基础分支逻辑，适合所有条件判断场景；
   - 单分支（if）、双分支（if-else）、多分支（if-elif-else）；
   - 三元运算符简化简单双分支：`结果1 if 条件 else 结果2`；
   - 核心避坑：缩进、冒号、`==`vs`=`。

2. **match-case（3.10+）**：
   - 高效模式匹配，适合字面量、数据类型、数据结构的匹配；
   - 支持多模式匹配（`|`）、通配符（`_`/`*`）、守卫条件（`if`）；
   - 核心避坑：版本兼容、`_`放在最后、字典匹配是“包含”而非“完全匹配”。

3. **选型原则**：
   - 简单条件用if-elif-else，多模式匹配用match-case；
   - 低版本Python只能用if-elif-else。

流程控制是让程序“有逻辑”的核心，后续的循环、函数、类都离不开分支判断——建议你把实战案例改成“用户登录验证”“商品价格计算”等场景，加深对分支逻辑的理解，为后续的复杂程序开发打下基础～

下一节，咱们会学习**Python循环结构（for/while）**：让程序“重复执行指定逻辑”，比如批量处理列表元素、循环爬取网页数据、实现定时任务，是自动化操作的核心技巧～


# 七、专栏订阅
> - <font color=black size=3>==专栏优点？==《Python从入门到实战》，专栏内容涵盖：Python基础到高级编程、Web开发（Django/Flask框架）、数据库（MySQL/ORM）、网络爬虫、Linux部署运维等全栈核心知识，以项目驱动教学，构建清晰学习路径，适合零基础入门和进阶提升的同学，跟着一步步从入门到精通！专栏地址：<https://blog.csdn.net/zsh_1314520/category_13108073.html></font>
> - <font color=black size=3>==文章是永久吗？== 一次订阅后，专栏内所有文章可永久免费查看，后续会持续更新全栈相关内容，第一时间获取最新教程！</font>
> - <font color=black size=3>==有答疑交流群吗？== 订阅专栏后有专属的全栈学习答疑群，群内提供专业问题答疑、和众多学习者抱团取暖，一起沉淀技术、赋能成长！</font>
> - <font color=black size=3>==进群方式？== 订阅专栏后可直接在专栏内申请加入答疑群，或私信博主沟通进群事宜：<https://bbs.csdn.net/topics/620104702></font>
> - <font color=black size=3>==更多干货？== 点赞+收藏+关注博主不迷路！博主博客链接：<https://blog.csdn.net/zsh_1314520?spm=1000.2115.3001.5343>，专注Python全栈技术分享，评论区留言问题会一一回复，助力大家轻松搞定Python全栈！</font>

【原创声明】

除本文原文地址以外，如发现同款内容皆为盗版，本文已收录于《[Python全栈：从入门到实战](https://blog.csdn.net/zsh_1314520/category_13108073.html)》 ，请勿购买盗版文章和专栏，如购买盗版内容不提供任何服务。原文地址：<https://blog.csdn.net/zsh_1314520/article/details/157359983>
