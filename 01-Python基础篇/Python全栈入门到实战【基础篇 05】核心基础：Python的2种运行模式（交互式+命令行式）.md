哈喽各位小伙伴！上一节咱们吃透了Python的变量与4种基础数据类型，掌握了“数据怎么存、怎么分类、怎么转换”的核心逻辑。但有个关键问题还没解决：**写好的Python代码，到底该怎么运行？**

很多新手刚接触Python时都会陷入困惑：为什么有时候在小黑窗里敲一行代码就能出结果，有时候却要把代码存成`.py`文件才能运行？这其实就是Python的两种核心运行模式在起作用——「交互式模式」和「命令行式模式（脚本式）」。

这两种模式没有“优劣之分”，只有“场景之别”：交互式适合**快速测试语法、验证小段代码**（比如临时验证`0.1+0.2`的结果、测试`type()`函数用法）；命令行式适合**运行完整程序、实现复杂功能**（比如上一节的“个人信息管理程序”）。

今天咱们就用“原理+实操+场景”的方式，把这两种运行模式讲透，包括：每种模式的启动方式、核心特点、适用场景、避坑要点，以及如何根据需求灵活切换——吃透这部分，你才能真正把“写代码”和“运行代码”打通，不再卡在“代码写好了却跑不起来”的新手坑里～


# 一、前置引入：为什么运行模式是入门的“必经之路”？
咱们上一节写的“个人信息综合管理程序”，如果直接在小黑窗里一行行敲，不仅容易出错，还没法保存；但如果存成`.py`文件，就能反复运行、修改，这就是两种模式的核心差异。

简单说：
- 没有交互式模式，你没法快速验证“某个语法对不对”（比如想知道`10//3`的结果，总不能每次都写个完整文件）；
- 没有命令行式模式，你没法实现“完整功能”（比如批量处理数据、开发小程序）。

这两种模式是Python代码执行的“两条核心路径”，新手必须同时掌握：
- 初学语法时：用交互式模式“边学边测”，快速理解语法规则；
- 实现功能时：用命令行式模式“编写脚本”，运行完整程序。

所以，这节内容不是“可选知识点”，而是“打通代码编写与执行的关键”，学好了能让你的Python学习效率翻倍～


# 二、交互式模式（REPL）：边敲代码边看结果，快速测试神器
## 1. 什么是交互式模式？
交互式模式（也叫REPL模式）是Python提供的“即时反馈”运行方式：**你输入一行代码，Python解释器就立即执行一行，并返回结果**，不用创建文件，不用写完整程序，是新手测试语法、验证逻辑的最佳工具。

### （1）REPL的含义
REPL是四个英文单词的缩写，对应交互式模式的核心流程：
- **R**ead（读取）：Python解释器读取你输入的代码；
- **E**valuate（执行）：解释器执行读取的代码；
- **P**rint（输出）：输出代码执行的结果；
- **L**oop（循环）：重复上述过程，直到你主动退出。

这个流程就像“和Python对话”——你问一个问题（输入代码），Python立刻回答（输出结果），特别适合新手边学边试。

### （2）启动交互式模式（Windows/macOS/Linux通用）
启动方式非常简单，核心是打开“终端/命令提示符”，输入`python`或`python3`命令：

#### 步骤1：打开终端/命令提示符
- **Windows系统**：
  1. 按下`Win + R`，输入`cmd`，回车打开“命令提示符”（小黑窗）；

  ![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/331132fde970485386fe6f875cab374e.gif#pic_center)


  2. 或在开始菜单搜索“命令提示符”/“PowerShell”，打开即可。

  ![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/2271ecc4aa1e46dbad9729635744a566.gif#pic_center)

- **macOS系统**：
  1. 按下`Command + 空格`，输入`Terminal`，回车打开“终端”；
  2. 或在“应用程序→实用工具”中找到“终端”。
- **Linux系统**：
  1. 按下`Ctrl + Alt + T`，直接打开终端；
  2. 或在应用菜单搜索“Terminal”。

#### 步骤2：输入命令启动Python交互式模式
在终端/命令提示符中输入以下命令，回车即可启动：
```bash
# 方式1：优先使用（推荐）
python3  # 适用于macOS/Linux，或Windows安装了Python3并配置了环境变量
# 方式2：备用（Windows常用）
python   # 若系统默认Python3，可直接用这个命令
```

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/d2974cbfcfbb4913bb985d92d85ef841.png#pic_center)


#### 启动成功的标志

终端会显示Python版本信息，并出现`>>>`提示符（这是交互式模式的核心标识），示例：

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/aa9008ff745f49928f89dc8ec7675ea9.png#pic_center)


看到`>>>`就说明启动成功了，接下来就可以输入代码，按回车立即执行。

#### 退出交互式模式
如果想退出，有两种方式：
1. 输入`exit()`，回车；

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/e4a62e8ab5ad42aea8d2a621ddd3a653.png#pic_center)


2. 按下快捷键：Windows/Linux按`Ctrl + Z`+回车，macOS按`Ctrl + D`。

### （3）交互式模式的核心特点
| 特点       | 具体说明                                       | 对新手的意义                                        |
| ---------- | ---------------------------------------------- | --------------------------------------------------- |
| 即时反馈   | 输入一行执行一行，立即看到结果                 | 快速验证语法，比如测试`type(100)`、`0.1+0.2`的结果  |
| 无需文件   | 不用创建`.py`文件，直接在终端输入              | 省去“新建文件→写代码→保存→运行”的步骤，适合临时测试 |
| 保留上下文 | 之前输入的变量、函数会被保留，后续可直接调用   | 比如先定义`a=10`，再输入`a+5`，能直接得到15         |
| 仅临时生效 | 退出后，所有输入的代码和变量都会消失，无法保存 | 不能用于运行完整程序，仅适合临时测试                |
| 有语法提示 | 输入代码时，按Tab键可自动补全（部分环境支持）  | 减少拼写错误，提升测试效率                          |

### （4）交互式模式实操示例
启动交互式模式后，跟着输入以下代码，感受“即时反馈”的特点：

#### 示例1：测试基础语法（变量、数据类型）
```python
>>> # 定义变量（输入后回车，无输出，变量已保存）
>>> user_age = 25
>>> # 查看变量值（输入后回车，立即输出）
>>> user_age
25
>>> # 测试数据类型
>>> type(user_age)
<class 'int'>
>>> # 测试数学运算
>>> user_age + 1
26
>>> # 测试浮点数精度误差
>>> 0.1 + 0.2
0.30000000000000004
>>> # 测试字符串拼接
>>> "你好，" + "张三"
'你好，张三'
>>> # 测试布尔值判断
>>> 25 >= 18
True
```

运行结果，如下图所示：

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/1a3611c361444612b9283b7c2c7cff12.png#pic_center)


#### 示例2：测试类型转换

```python
>>> # 字符串转整数
>>> int("100")
100
>>> # 浮点数转整数（舍弃小数）
>>> int(3.9)
3
>>> # 数字转字符串
>>> str(25)
'25'
>>> # 测试空字符串的布尔值
>>> bool("")
False
```

运行结果，如下图所示：

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/22380c8329ac45eaa1590407d8ca8269.png#pic_center)


#### 示例3：测试逻辑运算

```python
>>> # 定义布尔变量
>>> is_pass = True
>>> has_vip = False
>>> # 测试and运算
>>> is_pass and has_vip
False
>>> # 测试or运算
>>> is_pass or has_vip
True
>>> # 测试not运算
>>> not is_pass
False
```

运行结果，如下图所示：

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/d930c8a3cee3427cb8d7013aa41aed02.png#pic_center)


### （5）交互式模式的适用场景

交互式模式不是“万能的”，只适合以下场景：
1. **语法测试**：学习新语法时，快速验证是否正确（比如刚学`//`整除，测试`25//10`的结果）；
2. **逻辑验证**：验证小段逻辑是否符合预期（比如验证`18.5 <= bmi <=23.9`的判断结果）；
3. **临时计算**：当作“高级计算器”使用（比如计算`100*0.8*1.09`，比系统计算器更灵活）；
4. **帮助查询**：输入`help(函数名)`查看用法（比如`help(print)`，查看print函数的使用说明）。

### （6）交互式模式避坑要点
1. **不要写多行复杂代码**：交互式模式适合单行/少量代码测试，写循环、判断等多行代码时，容易出错且无法保存；
2. **注意缩进**：如果输入需要缩进的代码（比如`if`判断），换行后会出现`...`提示符，需手动按空格缩进（通常4个空格）：
   ```python
   >>> # 示例：if判断（注意缩进）
   >>> age = 25
   >>> if age >= 18:
   ...     print("成年")  # 这里要按4个空格缩进，再输入代码
   ...
   成年
   ```
3. **退出后数据丢失**：所有变量、代码都会消失，若需要保留，需复制到文件中；
4. **环境一致性**：确保启动的Python版本和你安装的一致（比如用`python3`启动Python3，避免用`python`启动Python2）。


# 三、命令行式模式（脚本式）：运行完整程序，实现复杂功能
## 1. 什么是命令行式模式？
命令行式模式（也叫脚本式模式）是Python最常用的运行方式：**将代码写入`.py`后缀的文本文件（称为Python脚本），然后在终端/命令提示符中执行这个文件**，Python解释器会从头到尾执行文件中的所有代码，适合实现完整功能。

### （1）核心原理
1. 你创建一个`.py`文件，把所有代码按逻辑写入并保存；
2. 在终端中执行`python3 文件名.py`（或`python 文件名.py`）；
3. Python解释器读取文件中的所有代码，按顺序执行；
4. 执行完成后，输出结果（若有`print`语句），解释器退出。

这个流程就像“给Python写一份‘任务清单’”，Python会按清单一步步执行，完成所有任务。

### （2）命令行式模式的核心步骤
以编写“简易计算器程序”为例，完整演示命令行式模式的使用流程：

#### 步骤1：创建Python脚本文件
- **方式1（新手推荐）**：用PyCharm创建
  1. 打开PyCharm，新建项目（比如`Python_Basic`）；
  2. 右键项目→New→Python File，命名为`calculator.py`；
  3. 在文件中写入以下代码：
     ```python
     # 简易计算器程序（命令行式模式示例）
     print("===== 简易计算器 =====")
     num1 = float(input("请输入第一个数字："))
     num2 = float(input("请输入第二个数字："))
     operator = input("请输入运算符（+/-/*/÷）：")

     # 根据运算符计算结果
     if operator == "+":
         result = num1 + num2
         print(f"{num1} + {num2} = {result}")
     elif operator == "-":
         result = num1 - num2
         print(f"{num1} - {num2} = {result}")
     elif operator == "*":
         result = num1 * num2
         print(f"{num1} * {num2} = {result}")
     elif operator == "÷":
         if num2 == 0:
             print("错误：除数不能为0！")
         else:
             result = num1 / num2
             print(f"{num1} ÷ {num2} = {result:.2f}")
     else:
         print("错误：请输入正确的运算符（+/-/*/÷）！")

     print("===== 程序结束 =====")
     ```
  4. 保存文件（PyCharm会自动保存，或按`Ctrl + S`）。

- **方式2（备用）**：用记事本/文本编辑器创建
  1. 打开记事本（Windows）/文本编辑（macOS）；
  2. 粘贴上述代码，点击“保存”；
  3. 保存类型选择“所有文件”，文件名写`calculator.py`（注意后缀是`.py`，不是`.txt`）；
  4. 记住保存路径（比如Windows保存到`桌面`，macOS保存到`文档`）。

#### 步骤2：打开终端/命令提示符，切换到文件所在目录
这是新手最容易卡壳的步骤——**必须先切换到脚本文件所在的目录，才能运行文件**。

##### 查看文件路径
- **PyCharm用户**：右键`calculator.py`→Copy Path/Reference→Copy Absolute Path（复制绝对路径）；
- **记事本/文本编辑用户**：找到保存的文件，右键→属性（Windows）/显示简介（macOS），查看“位置/路径”。

##### 切换目录（cd命令）
在终端/命令提示符中输入`cd 文件所在目录路径`，回车：
- **Windows示例（文件在桌面）**：
  ```bash
  # 切换到桌面目录（注意替换为你的用户名）
  cd C:\Users\你的用户名\Desktop
  ```
- **macOS/Linux示例（文件在文档）**：
  ```bash
  # 切换到文档目录
  cd ~/Documents
  ```

##### 验证是否切换成功
输入`dir`（Windows）或`ls`（macOS/Linux），回车，能看到`calculator.py`文件，说明切换成功。

#### 步骤3：执行脚本文件
在终端/命令提示符中输入以下命令，回车运行：
```bash
# 方式1（推荐）
python3 calculator.py
# 方式2（备用）
python calculator.py
```

#### 步骤4：查看运行结果
终端会提示输入内容，按提示输入即可，示例：
```bash
===== 简易计算器 =====
请输入第一个数字：10
请输入第二个数字：3
请输入运算符（+/-/*/÷）：÷
10.0 ÷ 3.0 = 3.33
===== 程序结束 =====
```

### （3）命令行式模式的核心特点
| 特点         | 具体说明                                           | 对新手的意义                                     |
| ------------ | -------------------------------------------------- | ------------------------------------------------ |
| 批量执行     | 一次性执行文件中的所有代码，按顺序运行             | 实现完整功能，比如计算器、个人信息管理程序       |
| 可保存复用   | 代码保存在`.py`文件中，可反复运行、修改            | 不用重复输入代码，适合开发稳定的程序             |
| 无即时反馈   | 必须运行整个文件才能看到结果，单行代码不会即时执行 | 写代码时需注意逻辑顺序，避免语法错误导致程序崩溃 |
| 支持复杂逻辑 | 可写循环、判断、函数等多行/复杂代码                | 实现交互式模式无法完成的功能（比如批量处理数据） |
| 输出可控     | 通过`print`语句控制输出内容，无多余提示符          | 输出结果更整洁，适合展示程序运行效果             |

### （4）命令行式模式的适用场景
命令行式模式是Python开发的“主力模式”，适合所有需要实现完整功能的场景：
1. **开发完整程序**：比如计算器、个人信息管理系统、数据处理脚本；
2. **批量处理任务**：比如批量读取文件、批量修改数据；
3. **项目开发**：Web开发、爬虫开发、数据分析等，都需要写脚本文件运行；
4. **代码复用**：将常用功能写成脚本，需要时直接运行（比如每月统计一次数据）。

### （5）命令行式模式避坑要点
1. **路径问题（高频坑）**：
   - 报错：`FileNotFoundError: [Errno 2] No such file or directory: 'calculator.py'`；
   - 原因：终端当前目录不是脚本文件所在目录；
   - 解决：用`cd`命令切换到正确目录，或使用绝对路径运行（`python3 C:\Users\用户名\Desktop\calculator.py`）。

2. **语法错误（高频坑）**：
   - 报错：`SyntaxError: invalid syntax`；
   - 原因：文件中的代码有语法错误（比如缩进不对、少写冒号）；
   - 解决：检查代码，尤其是缩进、冒号、引号，修正后重新运行。

3. **编码问题（Windows偶发）**：
   - 报错：`UnicodeDecodeError: 'gbk' codec can't decode byte...`；
   - 原因：文件包含中文，编码不兼容；
   - 解决：在文件开头添加编码声明：
     ```python
     # -*- coding: utf-8 -*-
     # 简易计算器程序（命令行式模式示例）
     print("===== 简易计算器 =====")
     # 后续代码不变
     ```

4. **Python版本问题**：
   - 报错：`SyntaxError: invalid syntax`（比如f-string报错）；
   - 原因：用Python2运行了Python3的代码（f-string是Python3.6+特性）；
   - 解决：用`python3`命令运行，而非`python`。

5. **文件名/路径含空格**：
   - 若文件路径或文件名含空格，需用引号包裹（比如`python3 "我的计算器.py"`）；
   - 避坑建议：文件名和路径尽量不用空格、中文（新手阶段），用下划线代替（比如`my_calculator.py`）。


# 四、两种运行模式的对比与选择
为了让你快速判断“该用哪种模式”，整理了清晰的对比表，核心是“按需选择”：

| 对比维度   | 交互式模式（REPL）           | 命令行式模式（脚本式）                   |
| ---------- | ---------------------------- | ---------------------------------------- |
| 核心流程   | 输入一行→执行一行→输出结果   | 写文件→保存→执行文件→输出结果            |
| 代码保存   | 退出后丢失，无法保存         | 保存在`.py`文件中，永久复用              |
| 适用代码量 | 单行/少量代码（≤10行）       | 多行/复杂代码（≥10行）                   |
| 适用场景   | 语法测试、临时计算、帮助查询 | 完整程序、批量任务、项目开发             |
| 反馈速度   | 即时反馈，快速验证           | 需运行文件，反馈较慢                     |
| 错误影响   | 仅影响当前行，可立即修正     | 语法错误会导致程序终止，需修正后重新运行 |
| 新手友好度 | 高（边学边试）               | 中等（需掌握路径、文件操作）             |

### 选择原则
1. **学新语法时**：先用交互式模式测试（比如学`for`循环，先在`>>>`里试`for i in range(5): print(i)`）；
2. **验证通过后**：把代码整理到`.py`文件中（命令行式），实现完整功能；
3. **临时计算/验证**：用交互式模式；
4. **实现完整功能**：用命令行式模式。


# 五、实战案例：两种模式结合使用
以“计算BMI指数”为例，演示“交互式测试→脚本式运行”的完整流程，这是新手学习的最佳实践：

### 步骤1：交互式模式测试核心逻辑
启动交互式模式，测试BMI计算逻辑是否正确：
```python
>>> # 测试BMI公式：体重(kg) ÷ 身高(m)²
>>> weight = 65
>>> height = 1.75
>>> bmi = weight / (height **2)
>>> bmi
21.224489795918366
>>> # 测试BMI判断逻辑
>>> 18.5 <= bmi <=23.9
True
```

### 步骤2：编写脚本文件（bmi_calculator.py）
将验证后的逻辑，扩展为完整程序，写入文件：
```python
# -*- coding: utf-8 -*-
"""
BMI指数计算器（两种模式结合示例）
步骤：1. 交互式测试核心逻辑 → 2. 脚本式实现完整功能
"""
print("===== BMI指数计算器 =====")
# 获取用户输入（带类型转换）
try:
    height = float(input("请输入你的身高（米，如1.75）："))
    weight = float(input("请输入你的体重（公斤，如65.5）："))
    
    # 计算BMI指数（核心逻辑来自交互式测试）
    bmi = weight / (height** 2)
    
    # 判断BMI等级
    if bmi < 18.5:
        level = "偏瘦"
    elif 18.5 <= bmi <= 23.9:
        level = "正常"
    elif 24.0 <= bmi <= 27.9:
        level = "超重"
    else:
        level = "肥胖"
    
    # 输出结果
    print(f"\n你的BMI指数：{bmi:.2f}")
    print(f"BMI等级：{level}")
    print("健康提示：正常BMI范围为18.5-23.9，建议保持均衡饮食和适量运动～")

except ValueError:
    print("错误：请输入有效的数字（如1.75、65）！")
except ZeroDivisionError:
    print("错误：身高不能为0！")
print("===== 程序结束 =====")
```

### 步骤3：命令行式运行脚本
按之前的步骤，切换到文件目录，执行：
```bash
python3 bmi_calculator.py
```

### 运行结果示例
```bash
===== BMI指数计算器 =====
请输入你的身高（米，如1.75）：1.75
请输入你的体重（公斤，如65.5）：65

你的BMI指数：21.22
BMI等级：正常
健康提示：正常BMI范围为18.5-23.9，建议保持均衡饮食和适量运动～
===== 程序结束 =====
```

### 案例核心价值
这个流程是Python开发的“标准流程”：
- 先用交互式模式“快速验证核心逻辑”，避免写完整文件后发现逻辑错误；
- 再用命令行式模式“封装完整功能”，添加输入验证、异常处理、友好提示，让程序更健壮。


# 六、常见问题排查
整理了新手运行代码时最容易遇到的5个高频问题，包含报错原因、解决方案和避坑建议：

1. 启动Python报错「'python' 不是内部或外部命令，也不是可运行的程序」
   - 报错原因：Python未配置环境变量，系统找不到Python解释器；
   - 解决方案：
     1. 重新安装Python，勾选“Add Python to PATH”（Windows）；
     2. 手动配置环境变量（Windows）：将Python安装路径（如`C:\Python311`）添加到系统环境变量`Path`中；
     3. macOS/Linux：用`which python3`查看Python路径，确保路径正确；
   - 避坑建议：安装Python时，务必勾选“添加到环境变量”，避免后续手动配置。

2. 运行脚本报错「FileNotFoundError: No such file or directory」
   - 报错原因：终端当前目录不对，或文件名输入错误（比如`calculator.py`写成`calculator.PY`）；
   - 解决方案：
     1. 用`cd`命令切换到脚本所在目录；
     2. 用`dir`/`ls`命令确认文件存在；
     3. 检查文件名拼写（Python区分大小写，比如`Calculator.py`和`calculator.py`是不同文件）；
   - 避坑建议：新手可直接用绝对路径运行（`python3 C:\Users\用户名\Desktop\calculator.py`），避免目录切换错误。

3. 运行脚本报错「SyntaxError: invalid syntax」
   - 报错原因：文件中有语法错误（如缩进不对、少写冒号、引号不匹配）；
   - 解决方案：
     1. 对照报错信息中的行号，检查对应行的代码；
     2. 重点检查：`if/for/while`后是否有冒号、缩进是否统一（4个空格）、引号是否成对；
     3. 先在交互式模式中测试报错行的代码，验证语法是否正确；
   - 避坑建议：写代码时，每写一小段就保存并运行，及时发现语法错误。

4. 中文乱码/编码报错「UnicodeDecodeError」
   - 报错原因：文件编码不兼容（Windows默认GBK，Python默认UTF-8）；
   - 解决方案：
     1. 在文件开头添加编码声明：`# -*- coding: utf-8 -*-`；
     2. 保存文件时，选择“UTF-8”编码（PyCharm默认UTF-8，记事本保存时选择“编码→UTF-8”）；
   - 避坑建议：所有Python脚本文件都在开头添加编码声明，避免中文乱码。

5. 交互式模式和脚本模式结果不一致
   - 报错原因：两种模式的Python版本不同（比如交互式是Python3，脚本式是Python2）；
   - 解决方案：
     1. 交互式模式输入`import sys; print(sys.version)`，查看版本；
     2. 脚本式模式运行`python3 --version`，查看版本；
     3. 统一用`python3`启动交互式和脚本模式；
   - 避坑建议：始终用`python3`命令，避免混用`python`（可能指向Python2）。


# 七、总结
今天咱们吃透了Python的两种核心运行模式，核心要点梳理如下：

1. **交互式模式（REPL）**：
   - 核心：输入一行执行一行，即时反馈，无需文件；
   - 启动：终端输入`python3`/`python`，看到`>>>`即为成功；
   - 适用场景：语法测试、临时计算、帮助查询；
   - 核心避坑：不要写复杂多行代码，退出后数据丢失。

2. **命令行式模式（脚本式）**：
   - 核心：将代码写入`.py`文件，终端执行文件，批量运行；
   - 步骤：创建文件→切换目录→执行`python3 文件名.py`；
   - 适用场景：完整程序、批量任务、项目开发；
   - 核心避坑：注意路径切换、语法错误、编码问题。

3. **选择原则**：
   - 学新语法/临时验证：用交互式模式；
   - 实现完整功能/复用代码：用命令行式模式；
   - 最佳实践：先交互式测试核心逻辑，再脚本式封装完整功能。

这两种模式是Python代码执行的“基础中的基础”，后续学习循环、函数、类等知识点时，都需要交替使用——交互式测试语法，脚本式实现功能。建议大家多动手练习：把上一节的“个人信息管理程序”用两种模式分别运行，感受差异，彻底掌握～

下一节，咱们会学习「Python的流程控制（if判断/for循环/while循环）」，这是Python实现“逻辑决策”和“重复操作”的核心语法，掌握后就能写出更复杂、更实用的程序啦～


# 八、专栏订阅
> - <font color=black size=3>==专栏优点？==《Python从入门到实战》，专栏内容涵盖：Python基础到高级编程、Web开发（Django/Flask框架）、数据库（MySQL/ORM）、网络爬虫、Linux部署运维等全栈核心知识，以项目驱动教学，构建清晰学习路径，适合零基础入门和进阶提升的同学，跟着一步步从入门到精通！专栏地址：<https://blog.csdn.net/zsh_1314520/category_13108073.html></font>
> - <font color=black size=3>==文章是永久吗？== 一次订阅后，专栏内所有文章可永久免费查看，后续会持续更新全栈相关内容，第一时间获取最新教程！</font>
> - <font color=black size=3>==有答疑交流群吗？== 订阅专栏后有专属的全栈学习答疑群，群内提供专业问题答疑、和众多学习者抱团取暖，一起沉淀技术、赋能成长！</font>
> - <font color=black size=3>==进群方式？== 订阅专栏后可直接在专栏内申请加入答疑群，或私信博主沟通进群事宜：<https://bbs.csdn.net/topics/620104702></font>
> - <font color=black size=3>==更多干货？== 点赞+收藏+关注博主不迷路！博主博客链接：<https://blog.csdn.net/zsh_1314520?spm=1000.2115.3001.5343>，专注Python全栈技术分享，评论区留言问题会一一回复，助力大家轻松搞定Python全栈！</font>

【原创声明】

除本文原文地址以外，如发现同款内容皆为盗版，本文已收录于《[Python全栈：从入门到实战](https://blog.csdn.net/zsh_1314520/category_13108073.html)》 ，请勿购买盗版文章和专栏，如购买盗版内容不提供任何服务。原文地址：<https://blog.csdn.net/zsh_1314520/article/details/156698390>
