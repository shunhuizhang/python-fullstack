

# Python全栈入门到实战【基础篇 06】核心工具+数据结构：cd命令、列表(list)、元组(tuple)

哈喽各位小伙伴！上一节咱们掌握了Python的两种运行模式，其中**cd命令**是运行脚本时“绕不开的核心工具”——很多新手卡壳在“切换目录”这一步，导致脚本运行失败；而实际开发中，我们经常需要存储“多个数据”（比如10个学生的成绩、5个商品的名称），单个变量根本不够用，这就需要Python最基础的序列型数据结构：**列表(list)** 和**元组(tuple)**。

这节内容是“工具+数据结构”的组合拳：
- `cd`命令：解决“终端切换目录”的高频痛点，让你再也不会因为路径错误导致脚本运行失败；
- 列表(list)：Python最常用的**可变**序列，能存多个数据、还能随意增删改，是处理“动态数据集合”的首选；
- 元组(tuple)：Python的**不可变**序列，数据一旦定义就不能改，适合存储“固定不变的数据集”（比如坐标、配置参数）。

今天咱们依旧用“原理+实操+场景+避坑”的方式，把这三个核心知识点讲透——吃透`cd`命令，你能流畅运行任意路径的脚本；吃透列表和元组，你就能从“存单个数据”升级到“存多个数据”，真正解锁Python处理批量数据的能力～


# 一、前置引入：为什么这三个知识点是“入门刚需”？
上一节运行`calculator.py`时，你是不是卡在了“切换到文件目录”这一步？这就是`cd`命令没掌握的问题——它是终端操作的“基础中的基础”，所有和文件路径相关的操作都离不开它。

而列表和元组的价值更核心：
- 没有列表/元组，你要存储5个学生的成绩，得定义5个变量：`score1=90`、`score2=85`、`score3=95`…不仅繁琐，还没法批量处理；
- 有了列表，只需一行`scores = [90,85,95,88,92]`，就能存储所有成绩，还能一键排序、筛选、修改，效率直接翻倍。

简单说：`cd`命令是“运行代码的工具基础”，列表/元组是“处理批量数据的语法基础”，这三个知识点必须同时掌握～


# 二、cd命令：终端目录切换神器
`cd`是“change directory”的缩写，直译就是“切换目录”，是终端/命令提示符中最常用的命令，核心作用是**改变当前工作目录**——只有切换到脚本文件所在的目录，才能顺利运行脚本。

## 1. cd命令的核心原理
终端启动后，会有一个“默认工作目录”（比如Windows默认是`C:\Users\你的用户名`，macOS/Linux默认是`/Users/你的用户名`）。`cd`命令的作用就是把这个“默认目录”切换到你指定的目录，让终端“定位”到脚本文件的位置。

## 2. cd命令的基础语法
```bash
cd [目标目录路径]
```
- `cd`：命令本身，固定不变；
- `[目标目录路径]`：可选，不写则切换到“用户主目录”（Windows是`C:\Users\你的用户名`，macOS/Linux是`~`）；
- 路径分为两种：
  - **绝对路径**：从根目录开始的完整路径（比如Windows的`C:\Users\张三\Desktop`，macOS的`/Users/张三/Documents`）；
  - **相对路径**：相对于当前目录的路径（比如`./test`表示当前目录下的test文件夹，`../`表示上级目录）。

## 3. 不同系统的cd命令实操
### （1）Windows系统（命令提示符/PowerShell）
#### 实操1：切换到桌面（绝对路径）
```bash
# 替换“张三”为你的用户名
cd C:\Users\张三\Desktop
# 验证：查看当前目录下的文件
dir
```
#### 实操2：切换到上级目录（相对路径）
```bash
# 假设当前在桌面（C:\Users\张三\Desktop）
# 切换到用户主目录（C:\Users\张三）
cd ..
# 验证
dir
```
#### 实操3：切换到子目录（相对路径）
```bash
# 假设桌面有个“Python脚本”文件夹
cd .\Python脚本
# 简化写法（省略./）
cd Python脚本
```
#### 实操4：切换到其他盘符
```bash
# 先输入盘符+冒号，再cd
D:
cd D:\项目\Python_Basic
```

### （2）macOS/Linux系统（终端）
#### 实操1：切换到文档目录（绝对路径）
```bash
# ~ 等价于 /Users/你的用户名
cd ~/Documents
# 验证：查看当前目录下的文件
ls
```
#### 实操2：切换到上级目录
```bash
# 假设当前在Documents，切换到用户主目录
cd ..
```
#### 实操3：切换到子目录
```bash
# 假设Documents有个“Python_Code”文件夹
cd Python_Code
```
#### 实操4：切换到根目录
```bash
cd /
```

### （3）cd命令的快捷操作（高效技巧）
| 命令    | 作用               | 适用系统    |
| ------- | ------------------ | ----------- |
| `cd`    | 切换到用户主目录   | 所有系统    |
| `cd ~`  | 切换到用户主目录   | macOS/Linux |
| `cd ..` | 切换到上级目录     | 所有系统    |
| `cd -`  | 切换到上一次的目录 | macOS/Linux |
| `cd /`  | 切换到根目录       | macOS/Linux |
| `盘符:` | 切换到指定盘符     | Windows     |

## 4. cd命令的避坑要点
### 坑1：路径含空格/中文
- 报错：`The system cannot find the path specified`；
- 原因：路径中有空格或中文，终端无法识别；
- 解决：用引号包裹路径：
  ```bash
  # Windows示例（路径含空格）
  cd "C:\Users\张三\我的 Python 脚本"
  # macOS/Linux示例（路径含中文）
  cd "~/Documents/我的代码"
  ```

### 坑2：路径拼写错误
- 报错：`File not found`；
- 解决：
  1. 用`dir`（Windows）/`ls`（macOS/Linux）查看当前目录的文件/文件夹，确认名称；
  2. 输入路径时按`Tab`键自动补全（终端支持路径补全，避免拼写错误）。

### 坑3：切换盘符忘记输“盘符:”
- 报错：切换到D盘的文件夹，但目录还是C盘；
- 解决：Windows系统切换其他盘符时，先输`D:`（或E:），再用`cd`切换目录。

### 坑4：相对路径使用错误
- 比如当前在`C:\Users\张三`，想切换到`C:\Users\张三\Desktop\test`，却输入`cd test`（少了Desktop）；
- 解决：要么用绝对路径，要么分步切换：
  ```bash
  cd Desktop
  cd test
  ```

## 5. 实战：用cd命令运行任意路径的脚本
假设`bmi_calculator.py`保存在`D:\Python_Project\Basic`，完整步骤：
```bash
# Windows系统
D:  # 切换到D盘
cd Python_Project\Basic  # 切换到脚本目录
dir  # 验证是否有bmi_calculator.py
python3 bmi_calculator.py  # 运行脚本

# macOS/Linux系统（假设脚本在/Users/张三/Projects/Python）
cd /Users/张三/Projects/Python
ls
python3 bmi_calculator.py
```


# 三、列表(list)：Python最常用的可变序列
列表是Python中**最核心、最常用**的数据结构，本质是“有序、可变的元素集合”——可以存储不同类型的数据（整数、字符串、布尔值甚至另一个列表），还能随意添加、删除、修改元素。

## 1. 列表的定义与基本格式
### （1）定义语法
用方括号`[]`包裹元素，元素之间用逗号`,`分隔：
```python
# 空列表
empty_list = []

# 存储同一类型数据（推荐）
scores = [90, 85, 95, 88, 92]  # 存储成绩（整数）
names = ["张三", "李四", "王五"]  # 存储姓名（字符串）

# 存储不同类型数据（允许但不推荐）
mix_list = [10, "张三", True, 3.14]
```

### （2）查看列表类型
```python
print(type(scores))  # 输出：<class 'list'>
```

## 2. 列表的核心操作（增删改查）
### （1）查：访问列表元素（索引/切片）
列表是**有序**的，每个元素都有一个“索引”（编号），索引从`0`开始。

#### ① 按索引访问单个元素
```python
names = ["张三", "李四", "王五"]
print(names[0])  # 输出：张三（第一个元素，索引0）
print(names[1])  # 输出：李四（第二个元素，索引1）
print(names[-1]) # 输出：王五（倒数第一个元素，索引-1）
```

#### ② 切片：访问多个元素
语法：`列表[起始索引:结束索引:步长]`（左闭右开，结束索引不包含）
```python
scores = [90, 85, 95, 88, 92]
print(scores[0:3])   # 输出：[90, 85, 95]（索引0-2）
print(scores[:3])    # 输出：[90, 85, 95]（省略起始索引，从0开始）
print(scores[2:])    # 输出：[95, 88, 92]（省略结束索引，到最后）
print(scores[::2])   # 输出：[90, 95, 92]（步长2，隔一个取一个）
print(scores[::-1])  # 输出：[92, 88, 95, 85, 90]（步长-1，反转列表）
```

### （2）改：修改列表元素
列表是**可变**的，直接通过索引赋值修改：
```python
scores = [90, 85, 95, 88, 92]
scores[1] = 88  # 将第二个元素（85）改为88
print(scores)   # 输出：[90, 88, 95, 88, 92]
```

### （3）增：添加元素到列表
| 方法     | 语法                      | 作用                             |
| -------- | ------------------------- | -------------------------------- |
| append() | `列表.append(元素)`       | 在列表**末尾**添加一个元素       |
| insert() | `列表.insert(索引, 元素)` | 在指定索引位置插入元素           |
| extend() | `列表.extend(另一个列表)` | 将另一个列表的元素批量添加到末尾 |

```python
names = ["张三", "李四", "王五"]

# append()：末尾添加
names.append("赵六")
print(names)  # 输出：["张三", "李四", "王五", "赵六"]

# insert()：指定位置插入
names.insert(1, "钱七")  # 在索引1的位置插入
print(names)  # 输出：["张三", "钱七", "李四", "王五", "赵六"]

# extend()：批量添加
names.extend(["孙八", "周九"])
print(names)  # 输出：["张三", "钱七", "李四", "王五", "赵六", "孙八", "周九"]
```

### （4）删：删除列表元素
| 方法     | 语法                  | 作用                                               |
| -------- | --------------------- | -------------------------------------------------- |
| del      | `del 列表[索引]`      | 删除指定索引的元素                                 |
| remove() | `列表.remove(元素值)` | 删除第一个匹配的元素值                             |
| pop()    | `列表.pop(索引)`      | 删除指定索引的元素并返回该元素（默认删除最后一个） |
| clear()  | `列表.clear()`        | 清空列表所有元素                                   |

```python
scores = [90, 85, 95, 88, 92]

# del：按索引删除
del scores[2]
print(scores)  # 输出：[90, 85, 88, 92]

# remove()：按值删除
scores.remove(85)
print(scores)  # 输出：[90, 88, 92]

# pop()：删除并返回
last_score = scores.pop()
print(last_score)  # 输出：92
print(scores)      # 输出：[90, 88]

# clear()：清空
scores.clear()
print(scores)      # 输出：[]
```

## 3. 列表的常用内置方法
| 方法      | 语法               | 作用                                           |
| --------- | ------------------ | ---------------------------------------------- |
| len()     | `len(列表)`        | 获取列表元素个数（长度）                       |
| sort()    | `列表.sort()`      | 对列表元素升序排序（原地修改）                 |
| sorted()  | `sorted(列表)`     | 对列表元素升序排序（返回新列表，不修改原列表） |
| reverse() | `列表.reverse()`   | 反转列表（原地修改）                           |
| index()   | `列表.index(元素)` | 返回第一个匹配元素的索引                       |
| count()   | `列表.count(元素)` | 返回元素在列表中出现的次数                     |

```python
scores = [90, 85, 95, 88, 92]

# len()：长度
print(len(scores))  # 输出：5

# sort()：升序排序
scores.sort()
print(scores)  # 输出：[85, 88, 90, 92, 95]

# sorted()：升序排序（返回新列表）
new_scores = sorted(scores, reverse=True)  # 降序排序
print(new_scores)  # 输出：[95, 92, 90, 88, 85]

# index()：查找索引
print(scores.index(90))  # 输出：2

# count()：统计次数
print(scores.count(88))  # 输出：1
```

## 4. 列表的适用场景
- 存储“动态变化”的数据集：比如购物车商品（可添加/删除）、学生成绩（可修改）、待办事项（可增删）；
- 批量处理数据：比如对成绩排序、筛选及格的学生、统计平均分；
- 作为函数返回值：比如函数返回多个结果，用列表封装。

## 5. 列表避坑要点
### 坑1：索引越界
- 报错：`IndexError: list index out of range`；
- 原因：访问的索引超过列表长度-1（比如列表有5个元素，索引最大是4，访问索引5就会报错）；
- 解决：访问前先判断索引是否合法，或用`len()`获取长度：
  ```python
  scores = [90, 85, 95]
  if len(scores) > 3:
      print(scores[3])
  else:
      print("索引越界，列表长度为：", len(scores))
  ```

### 坑2：修改不可哈希元素
- 列表可以存储任意类型，但不要尝试修改“不可变类型”（比如字符串）的元素（列表本身可变，和元素类型无关）；
- 示例（错误）：
  ```python
  names = ["张三", "李四"]
  names[0][0] = "张"  # 字符串不可变，报错：TypeError: 'str' object does not support item assignment
  ```

### 坑3：混淆append()和extend()
- `append()`添加单个元素（哪怕是列表，也会作为一个元素）：
  ```python
  a = [1,2]
  a.append([3,4])
  print(a)  # 输出：[1,2,[3,4]]
  ```
- `extend()`批量添加元素：
  ```python
  a = [1,2]
  a.extend([3,4])
  print(a)  # 输出：[1,2,3,4]
  ```


# 四、元组(tuple)：Python的不可变序列
元组是和列表类似的有序序列，但核心区别是：**元组一旦定义，元素不能修改（不可变）**。适合存储“固定不变”的数据集（比如坐标、配置参数、颜色值）。

## 1. 元组的定义与基本格式
### （1）定义语法
用圆括号`()`包裹元素，元素之间用逗号`,`分隔（空元组/单个元素元组有特殊写法）：
```python
# 空元组
empty_tuple = ()

# 存储多个元素（常规写法）
coords = (100, 200)  # 存储坐标（整数）
colors = ("red", "green", "blue")  # 存储颜色（字符串）

# 单个元素的元组（必须加逗号，否则不是元组）
single_tuple = (10,)  # 正确：元组
not_tuple = (10)      # 错误：只是整数
print(type(single_tuple))  # 输出：<class 'tuple'>
print(type(not_tuple))      # 输出：<class 'int'>

# 省略括号（元组的特殊写法，称为“元组解包”）
nums = 1,2,3
print(type(nums))  # 输出：<class 'tuple'>
```

### （2）查看元组类型
```python
print(type(coords))  # 输出：<class 'tuple'>
```

## 2. 元组的核心操作（只有“查”，没有增删改）
因为元组是**不可变**的，所以只能访问元素，不能修改、添加、删除元素。

### （1）按索引访问单个元素
```python
colors = ("red", "green", "blue")
print(colors[0])  # 输出：red
print(colors[-1]) # 输出：blue
```

### （2）切片访问多个元素
和列表切片语法完全一致：
```python
coords = (100, 200, 300, 400)
print(coords[1:3])  # 输出：(200, 300)
print(coords[::-1]) # 输出：(400, 300, 200, 100)
```

### （3）元组的常用内置方法
只有两个常用方法（因为不可变，方法比列表少）：
| 方法    | 语法               | 作用                     |
| ------- | ------------------ | ------------------------ |
| len()   | `len(元组)`        | 获取元组长度             |
| index() | `元组.index(元素)` | 返回第一个匹配元素的索引 |
| count() | `元组.count(元素)` | 返回元素出现的次数       |

```python
colors = ("red", "green", "blue", "red")
print(len(colors))       # 输出：4
print(colors.index("green"))  # 输出：1
print(colors.count("red"))    # 输出：2
```

## 3. 列表vs元组：核心区别与选择原则
| 特性     | 列表(list)                  | 元组(tuple)                    |
| -------- | --------------------------- | ------------------------------ |
| 可变性   | 可变（增删改）              | 不可变（仅能查）               |
| 语法     | 方括号`[]`                  | 圆括号`()`（单个元素需加逗号） |
| 性能     | 稍慢（需处理可变操作）      | 稍快（不可变，内存占用更小）   |
| 适用场景 | 动态数据（购物车、成绩）    | 固定数据（坐标、配置、常量）   |
| 内置方法 | 多（append/sort/reverse等） | 少（仅index/count）            |

### 选择原则
- 数据需要“增删改”→用列表；
- 数据“固定不变”→用元组；
- 函数返回多个值→默认返回元组（Python特性）；
- 字典的键→只能用元组（不可变），不能用列表（可变）。

## 4. 元组避坑要点
### 坑1：修改元组元素
- 报错：`TypeError: 'tuple' object does not support item assignment`；
- 原因：元组不可变，试图修改元素；
- 解决：若需修改，先转成列表，修改后再转回元组：
  ```python
  coords = (100, 200)
  # 转列表
  coords_list = list(coords)
  # 修改
  coords_list[1] = 300
  # 转回元组
  coords = tuple(coords_list)
  print(coords)  # 输出：(100, 300)
  ```

### 坑2：单个元素元组漏加逗号
- 错误写法：`t = (10)`（实际是整数）；
- 正确写法：`t = (10,)`（带逗号才是元组）。

### 坑3：元组中的可变元素
- 元组本身不可变，但如果元组中的元素是“可变类型”（比如列表），该元素可以修改：
  ```python
  t = (10, [20, 30])
  t[1][0] = 200  # 修改元组中的列表元素
  print(t)  # 输出：(10, [200, 30])
  ```


# 五、实战案例：学生成绩管理程序（cd+列表+元组）
整合本节所有知识点，编写一个“学生成绩管理程序”，涵盖`cd`命令运行脚本、列表存储动态成绩、元组存储固定配置：

### 步骤1：编写脚本（student_score.py）
```python
# -*- coding: utf-8 -*-
"""
学生成绩管理程序
核心知识点：列表（动态存储成绩）、元组（固定配置）
"""
print("===== 学生成绩管理系统 =====")

# 1. 元组：存储固定配置（及格线、优秀线）
SCORE_CONFIG = (60, 90)  # 不可变，及格线60，优秀线90
PASS_LINE, EXCELLENT_LINE = SCORE_CONFIG  # 元组解包

# 2. 列表：存储学生成绩（动态）
scores = [
    ("张三", 85),
    ("李四", 92),
    ("王五", 58),
    ("赵六", 95)
]

# 3. 展示所有成绩
print("\n【当前所有学生成绩】")
for name, score in scores:
    # 判断成绩等级
    if score >= EXCELLENT_LINE:
        level = "优秀"
    elif score >= PASS_LINE:
        level = "及格"
    else:
        level = "不及格"
    print(f"{name}：{score}分 → {level}")

# 4. 添加新学生成绩
new_name = input("\n请输入新增学生姓名：")
new_score = int(input("请输入新增学生成绩："))
scores.append((new_name, new_score))  # 列表添加元组

# 5. 删除不及格学生（可选）
delete_fail = input("\n是否删除不及格学生？(y/n)：")
if delete_fail.lower() == "y":
    # 筛选及格的学生
    scores = [s for s in scores if s[1] >= PASS_LINE]

# 6. 成绩排序（升序）
scores.sort(key=lambda x: x[1])

# 7. 展示最终成绩
print("\n【最终学生成绩（升序）】")
for name, score in scores:
    print(f"{name}：{score}分")

print("\n===== 程序结束 =====")
```

### 步骤2：用cd命令运行脚本
假设脚本保存在`C:\Users\张三\Desktop\Python_Code`：
```bash
# Windows命令提示符
cd C:\Users\张三\Desktop\Python_Code
dir  # 确认有student_score.py
python3 student_score.py
```

### 运行结果示例
```
===== 学生成绩管理系统 =====

【当前所有学生成绩】
张三：85分 → 及格
李四：92分 → 优秀
王五：58分 → 不及格
赵六：95分 → 优秀

请输入新增学生姓名：钱七
请输入新增学生成绩：88

是否删除不及格学生？(y/n)：y

【最终学生成绩（升序）】
张三：85分
钱七：88分
李四：92分
赵六：95分

===== 程序结束 =====
```

### 案例核心知识点覆盖
1. `cd`命令：切换到脚本目录，运行程序；
2. 元组：`SCORE_CONFIG`存储固定的及格线/优秀线，不可修改；
3. 列表：`scores`存储动态的学生成绩，支持添加、筛选、排序；
4. 元组解包：`PASS_LINE, EXCELLENT_LINE = SCORE_CONFIG`，简化变量赋值；
5. 列表推导式：`scores = [s for s in scores if s[1] >= PASS_LINE]`，筛选及格学生。


# 六、常见问题排查
整理了本节3个核心知识点的高频问题，帮助你快速定位解决：

### 1. cd命令相关问题
| 报错                      | 原因                                          | 解决方案                                                 |
| ------------------------- | --------------------------------------------- | -------------------------------------------------------- |
| `'cd' 不是内部或外部命令` | Windows用了错误的终端（比如Python交互式模式） | 退出Python交互式模式（输入exit()），在命令提示符中执行cd |
| `系统找不到指定的路径`    | 路径拼写错误/含空格未加引号                   | 1. 检查路径拼写；2. 用引号包裹含空格的路径               |
| `切换盘符后目录不变`      | Windows切换盘符时未输“盘符:”                  | 先输`D:`（或E:），再用cd切换目录                         |

### 2. 列表相关问题
| 报错                                                       | 原因                           | 解决方案                                        |
| ---------------------------------------------------------- | ------------------------------ | ----------------------------------------------- |
| `IndexError: list index out of range`                      | 索引超过列表长度-1             | 访问前用`len()`判断索引是否合法                 |
| `AttributeError: 'list' object has no attribute 'append'`  | 变量名不是列表（比如是字符串） | 检查变量类型：`print(type(变量名))`，确认是list |
| `TypeError: append() takes exactly one argument (2 given)` | append()传了多个参数           | 批量添加用extend()，单个添加用append()          |

### 3. 元组相关问题
| 报错                                                         | 原因                             | 解决方案               |
| ------------------------------------------------------------ | -------------------------------- | ---------------------- |
| `TypeError: 'tuple' object does not support item assignment` | 试图修改元组元素                 | 转列表修改后再转回元组 |
| `TypeError: 'int' object is not subscriptable`               | 单个元素元组漏加逗号，实际是整数 | 加逗号：`t = (10,)`    |


# 七、总结
今天咱们吃透了“工具+数据结构”的核心组合，核心要点梳理如下：

1. **cd命令**：
   - 核心作用：切换终端当前目录，解决脚本运行的路径问题；
   - 关键语法：`cd 目标路径`（绝对路径/相对路径）；
   - 避坑：路径含空格加引号，Windows切换盘符先输“盘符:”。

2. **列表(list)**：
   - 核心特性：有序、可变，存储动态数据；
   - 核心操作：索引/切片访问、append/insert添加、del/remove删除、sort排序；
   - 适用场景：购物车、成绩、待办事项等动态数据集。

3. **元组(tuple)**：
   - 核心特性：有序、不可变，存储固定数据；
   - 核心操作：仅支持索引/切片访问，无增删改；
   - 适用场景：坐标、配置、常量等固定数据集，单个元素需加逗号。

这三个知识点是Python入门的“关键拼图”：`cd`命令让你能流畅运行代码，列表/元组让你能处理批量数据。建议你修改实战案例的代码（比如添加成绩修改功能、筛选优秀学生），加深对列表和元组的理解～

下一节，咱们会学习「Python字典(dict)」——这是Python中最强大的映射型数据结构，能通过“键值对”存储数据（比如`{"name":"张三", "age":25}`），解决列表/元组“按索引访问不直观”的问题，让数据存储更贴合实际场景～


# 八、专栏订阅
> - <font color=black size=3>==专栏优点？==《Python从入门到实战》，专栏内容涵盖：Python基础到高级编程、Web开发（Django/Flask框架）、数据库（MySQL/ORM）、网络爬虫、Linux部署运维等全栈核心知识，以项目驱动教学，构建清晰学习路径，适合零基础入门和进阶提升的同学，跟着一步步从入门到精通！专栏地址：<https://blog.csdn.net/zsh_1314520/category_13108073.html></font>
> - <font color=black size=3>==文章是永久吗？== 一次订阅后，专栏内所有文章可永久免费查看，后续会持续更新全栈相关内容，第一时间获取最新教程！</font>
> - <font color=black size=3>==有答疑交流群吗？== 订阅专栏后有专属的全栈学习答疑群，群内提供专业问题答疑、和众多学习者抱团取暖，一起沉淀技术、赋能成长！</font>
> - <font color=black size=3>==进群方式？== 订阅专栏后可直接在专栏内申请加入答疑群，或私信博主沟通进群事宜：<https://bbs.csdn.net/topics/620104702></font>
> - <font color=black size=3>==更多干货？== 点赞+收藏+关注博主不迷路！博主博客链接：<https://blog.csdn.net/zsh_1314520?spm=1000.2115.3001.5343>，专注Python全栈技术分享，评论区留言问题会一一回复，助力大家轻松搞定Python全栈！</font>

【原创声明】

除本文原文地址以外，如发现同款内容皆为盗版，本文已收录于《[Python全栈：从入门到实战](https://blog.csdn.net/zsh_1314520/category_13108073.html)》 ，请勿购买盗版文章和专栏，如购买盗版内容不提供任何服务。原文地址：<https://blog.csdn.net/zsh_1314520/article/details/156719165>
